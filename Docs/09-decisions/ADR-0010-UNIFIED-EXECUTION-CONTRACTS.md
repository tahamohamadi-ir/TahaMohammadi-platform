# ADR-0010 — Unified execution contracts

Date: 2026-09-05. Status: Accepted implementation direction under the owner's instruction to reconcile all active documentation and tasks. Runtime, migration, deployment and visual acceptance remain OPEN.

The authoritative dispatch queue is `Docs/05-delivery/concept-alignment-v2/execution-tasks.json`; human entry point is EXECUTION.md in that directory. PU-01 coverage and PU-02 target-interface planning are completed by this reconciliation. They do not certify endpoint implementation. Parent PU labels are rollups, not worker assignments.

CA-01–08 remain bounded hero/gateway/shell packets. CA-09–16 become superseded aliases of explicitly listed PU family packets; their visual criteria transfer to those packets. CA-17 remains visual review after all active public implementation packets. All exact shared-file overlaps are serialized in the canonical graph. No agent is started by writing this plan.

`Docs/03-contracts/PRODUCT-INTERFACES-V2.md` finalizes target interface names, browser routes, existing endpoint reuse, additive schema work, preview/revisions, reference resolution, publication jobs and analytics. It distinguishes target contracts from source-generated current OpenAPI. Existing books/talks/downloads endpoints are reused. Graph references require a tested ID-to-slug resolver; no frontend guessing.

Keep Pagefind instead of creating a backend search service. Select first-party aggregate analytics on the existing backend for V1. Give research statements an unambiguous detail route. Keep Astro/TypeScript with bounded Three.js/GSAP and the existing admin/backend stacks; React is not an obligatory dependency for public interactions.

Current product/design/architecture/roadmap entry points and repository task registers are updated. Historical accepted ADRs, completed handoffs, pinned reference snapshots, source content and previous validation reports are retained as dated evidence; their old task-selection/freeze rules do not override this active queue. This is explicit supersession, not a rewrite of historical completion evidence.

Publication callbacks and runner contracts are selected for future implementation only. Their credentials and production activation remain environment work. No request to invent profile facts, publish content, commit, push or deploy is inferred.
