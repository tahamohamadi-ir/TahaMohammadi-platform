# Plan A ledger — `Docs/05-delivery/knowledge-atlas/plans/KNOWLEDGE-ATLAS-V1-PLAN-A-DOMAIN-API.md`

Plan: **KNOWLEDGE ATLAS V1 — PLAN A (DOMAIN MODEL + PUBLIC API)**.
This ledger belongs to Plan A only. Plans B/C/D keep their own.
Execution mode: subagent-driven development (fresh implementer context per task, spec-compliance + code-quality review, fix/re-review loop).

## 0. Phase 0 — binding documentation corrections (prerequisite)

| Item | Value |
|---|---|
| Correction commit | `08339ffd0336440cf5f7d0bcdf9d90333020285d` (`docs(atlas): close Atlas v1 implementation-plan gates`) |
| Files | spec + Plans A/B/C/D (5 paths, +262/−90), staged by explicit path |
| Correction 1 | Locale parity is a publish **blocker** in both directions (spec §20.1 note added; R3 wording fixed; Plan A Tasks 10/13 parity-matrix tests; Plan B validation-panel blocker tests; both plans' constraints) |
| Correction 2 | Preview token transport redesigned: `POST /api/v1/admin/atlas/versions/{id}/preview-token` mints `{preview_url: "/<locale>/atlas/preview/#token=…", expires_at, version_id}`; `GET /api/atlas/preview?locale=…` with `Authorization: Bearer`; fragment consumed and stripped with `history.replaceState()`; `no-store`/`no-cache`/`noindex, nofollow`/`no-referrer`; 401/403, never 404; signing secret backend-only (spec §10.10/§10.10.1, Plan A Task 15, Plan B Tasks 6/17, Plan C Tasks 5/8, Plan D row) |
| Correction 3 | One compact-overview field: `mobile_overview_priority` (model) / `mobileOverviewPriority` (wire); spec §5/§14.2/§15.2/§10.2/§10.3 + Plans A/B updated; no `mobile_overview`/`mobileOverview` alias remains |
| Phase 0 self-review | 7/7 checks pass (placeholders 0; §-refs resolve; A produces what B/C/D consume; parity never a warning; no token in path/query; one field name; no runtime file touched). Two real defects were found by the check and fixed before commit: Plan A's node field-set/test/`clean()` still used `mobile_overview`; Plan D's interface row did not name `GET /api/atlas/preview` |

## 1. Baseline (recorded before Task 1)

| Fact | Value |
|---|---|
| Primary repo (main checkout) | `D:/Project/tahamohammadi-platform/Back-End` — **never modified** by this plan |
| Main checkout branch / HEAD | `main` @ `6c0d349135536e9ca50dbe25c3f763985241c4f7` (`ci: use available setup-uv major tag`, 2026-09-10 12:55:58 −07:00) |
| `origin/main` | `6c0d349` — left/right `0 0` before the worktree branch |
| Main checkout status | **17 dirty entries** (pre-existing, untouched; captured §5) |
| Working worktree | `D:/Project/.atlas-worktrees/backend-plan-a` |
| Worktree branch | `feat/knowledge-atlas-domain-api` (created with `--no-track` from `main`) |
| Worktree base SHA | `6c0d349135536e9ca50dbe25c3f763985241c4f7` |
| Worktree status at start | clean (0 dirty) |
| Interpreter | `D:/Project/tahamohammadi-platform/Back-End/.venv/Scripts/python.exe` (Python 3.12.13, Django 5.2.17) |
| Baseline suite | `env -u PYTHONPATH DJANGO_SETTINGS_MODULE=config.settings.test <venv> -m pytest tests/ apps/ -q` → **946 passed, 6 skipped in 19.67s**, zero failures. 946 + 6 = 952 collected, matching the recon count. No pre-existing failure to classify |
| New public contract paths expected | `pathCount` 49 → 51 (`/api/atlas/{locale}`, `/api/atlas/preview`) |

## 2. Rulings (authority order: corrected spec → corrected Plan A → repo conventions)

| # | Ruling | Reason |
|---|---|---|
| R1 | The plan executes in a **linked worktree outside every repository**: `D:/Project/.atlas-worktrees/backend-plan-a`. | `Back-End/.gitignore` and the coordination root's `.gitignore` ignore no `.worktrees/` path, so an in-repo worktree would appear as untracked noise in an owner-active checkout. `D:/Project` is not a git repo, so the chosen directory is invisible to every repository — stronger than an ignore rule. The dirty main checkout is never touched. |
| R2 | The worktree uses the **main checkout's venv by absolute path** (no `.venv` inside the worktree, no junction). | `[tool.uv] package = false` and no editable install → the venv holds dependencies only, so running it with `cwd` = worktree imports `config`/`apps` from the worktree. A junction would add `sys.prefix` ambiguity for no benefit. The plan's `./.venv/Scripts/python.exe` stays literally valid in the main checkout. |
| R3 | Task 1 Step 5 runs the preflight against a **copy** of the development database **read-only**; no production data is touched and no repair is attempted. | Card: "do not mutate production data in this task". The development settings resolve the DB to `BASE_DIR / "dev.sqlite3"`, and a worktree has no such file — running there would silently create an empty database and produce a meaningless report. |
| R3a | Concretely: `cp Back-End/dev.sqlite3 <worktree>/dev.sqlite3` (the original is only read), then run `atlas_preflight --json` inside the worktree against that copy. Any DB file created in the worktree is git-ignored and never staged. | Same as R3; keeps the report real (1.6 MB of actual dev content) while leaving the owner's DB untouched. |
| R4 | `apps/atlas/tests/factories.py` is **created by the first task that needs shared builders — Task 6** (model helpers `_node`, `_node_type`), then extended by Task 7 (`_relation_type`), Task 8 (`seed_pairs`, `atlas_v1`, `atlas_active_version`, `atlas_two_versions`), Task 13/Task 15 as needed, and Task 18 (scale fixture). | The plan text implies the small `atlas_v1` family and the `_node`/`_relation_type` helpers without naming a creating task; consumers start at Task 6/7/9, while `factories.py` was only *created* in Task 8 and *modified* in Task 18. Creating it at the first consumer removes a forward dependency and keeps one home for builders. |
| R5 | `atlas_v1`, `atlas_active_version`, `atlas_two_versions` (with `en_missing_node`/`fa_missing_node`, `override()`, `add_draft_only_node()`, `break/restore_{en,fa}_projection()`) are created in **Task 8** on top of `factories.py` and reused unchanged by Tasks 9, 10, 12, 13, 15. | Phase 0 added these to Task 18's interface, which runs *after* its consumers; the corrected home is Task 8, where `factories.py` first gains canonical/`seed_pairs` fixtures. |
| R6 | Shared test files are extended additively and each task re-runs the **whole file**, never only its own `-k` selection. | `test_content_entities.py` (T2, T3) and `tests/test_atlas_public_api.py` (T15, T16) are touched by two tasks each; a `-k` run would hide the other task's tests. |
| R7 | Task 17 rewrites `ATLAS-PAYLOAD-CONTRACT.md` but **keeps** the Task 1 preflight report format section. | Task 1's produced interface requires that section; Task 17 documents the implemented projection. |
| R8 | Cross-repo contract work (Task 19) uses its own clean worktrees in `Front-End/public-site` (and `admin-panel` only when Plan B reaches it), each with branch `feat/knowledge-atlas-domain-api-contract`, committed per repository, never merged or pushed. | Card's CROSS-REPO CONTRACT TASKS section. |
| **R9** | **Public-key uniqueness is per version, not global.** `AtlasNode` and `AtlasGroup` carry `UniqueConstraint(["version", "public_key"])`; `AtlasRelation` has **no stored `public_key` column** — its composed key is a property, so its per-version uniqueness follows from the existing `(version, source, target, relation_type)` constraint plus per-version endpoint keys (documented in a `Meta` comment). Spec §5.3's "unique `public_key` globally" is **superseded** (amended in place with a supersede marker in the spec, the plan's Task 6 constraint snippet and its inverted test name). | **Proven contradiction:** spec §8.2's flow `Active vN ──clone──▶ Draft vN+1 ──edit──▶ Validate ──▶ Publish (atomic)` plus Plan A Task 13's `clone_version` ("New draft with copied public keys") plus the card's CRITICAL rule *"Stable public keys must not silently mutate"* require a clone to carry its source's keys while both versions coexist — which global uniqueness forbids. Task 7's implementer reproduced `IntegrityError` when copying a node key **and** a group key onto a coexisting version. Per-version uniqueness preserves the property global uniqueness was protecting (a key identifies exactly one entity in the *served* topology, because exactly one version is active at a time) and is the only shape compatible with clone-then-publish. **Owner-visible spec amendment — must be accepted or reversed before Plan B's clone action ships.** |
| R10 | Shared builders live in `apps/atlas/tests/factories.py` only; no second builders module, no `tests/__init__.py` (repo convention). | Task 6/7 practice; keeps R4's single home. |

## 3. Pre-flight plan scan (task/interface conflict table)

Scanned all 20 tasks: files, tests, produced/consumed interfaces, step counts. Every task names exact files, carries a test file, and declares its interfaces. 44 distinct paths are touched; 8 are shared by more than one task:

| Shared path | Tasks | Compatibility finding |
|---|---|---|
| `Back-End/apps/atlas/models.py` | T5 create → T6 modify → T7 modify | Additive model classes; each adds one migration. OK |
| `Back-End/apps/atlas/validation.py` | T9 create → T10 modify → T12 modify | Additive rule sets; T12 aggregates. OK |
| `Back-End/apps/api/api.py` | T15 modify → T16 modify | T15 registers both Atlas routes; T16 adds ETag/304 to the active route only. OK |
| `Back-End/apps/content/models.py`, `apps/content/admin.py` | T2 modify → T3 modify | Mirrored `Method`/`Technology` entities; T3 duplicates T2's assertions on purpose. OK |
| `Back-End/apps/atlas/tests/test_content_entities.py` | T2 test → T3 test | Additive test classes in one file (see R6). OK with R6 |
| `Back-End/tests/test_atlas_public_api.py` | T15 test → T16 test | Additive tests in one file (see R6). OK with R6 |
| `Back-End/docs/contracts/ATLAS-PAYLOAD-CONTRACT.md` | T1 modify → T17 modify | T17 supersedes prose, keeps the preflight format (see R7). OK with R7 |
| `Back-End/apps/atlas/tests/factories.py` | T6 create (R4) → T7/T8/T18 modify | One home for builders, created at first consumer. OK with R4/R5 |

No producer/consumer pair was found where a task consumes an interface produced by a *later* task, after R4/R5 were applied. No task deletes a path another task produces (Plan D owns all retirement).

## 4. Task log

| Task | Title | Status | Commit | Notes |
|---|---|---|---|---|
| 1 | Preflight — bilingual pairing audit (read-only) | **done** | `8f6ff2c` | RED `2 failed` (Unknown command) → GREEN `2 passed`; whole file, no `-k`. Full suite `948 passed, 6 skipped`. Commit = exactly 8 paths (+186), staged by explicit path. `ruff check apps/atlas/` → All checks passed. Preflight run against a **copy** of the dev DB (R3/R3a): `atlas_preflight: candidates=0 blocking=0`, exit 0 — **finding:** the dev DB has 0 rows in `content_profile`/`content_research_topic`, so §23.2 pairing risk can only be answered against the real content DB (Plan D prerequisite, reported not repaired). Owner's `dev.sqlite3` md5 `4e73e231…` identical before/after. Ruling: `Profile.translation_key` is a non-null `uuid4` default (not the mixin's nullable field) — the specified null/partner logic handles both, no change. |
| 2 | `Method` entity | **done** | `79b37ce` | RED `ImportError: cannot import name 'Method'` → GREEN `1 passed`; whole file. Full suite `949 passed, 6 skipped`; `ruff check .` → All checks passed; `makemigrations --check --dry-run` → No changes detected. Commit = 4 paths (+127): `apps/content/models.py` (`Method` mirroring `ResearchTopic`), `apps/content/admin.py`, `apps/content/migrations/0032_method.py`, `apps/atlas/tests/test_content_entities.py`. Rulings: admin registration split T2/T3 as the plan's two Files blocks imply (repo is post-DEBT-0003 — `content/admin.py` is a stub; docstring preserved); `__str__` added for convention parity; the plan's 102-char `UniqueConstraint` line wrapped to ruff's 100 limit; migration backout evidence collected on a **copy** of `dev.sqlite3` in `%TEMP%` (forward → `content_method=True`, `migrate content 0031` → unapplied, forward again → recreated; worktree DB mtime unchanged). |
| — | Plan nit recorded | — | — | The File Map lists `apps/atlas/tests/__init__.py`, but **no test package in this repository has one** (`apps/content/tests`, `apps/api/tests` have none) — ruling: do not create it; convention wins. `apps/atlas/tests/factories.py` still arrives at Task 6 per R4. |
| 3 | `Technology` entity | **done** | `587168e` | RED `ImportError: cannot import name 'Technology'` → GREEN `3 passed` (the plan said `2 passed`; Task 3 adds two tests and re-proves Task 2's — plan text undercounted). Full suite `951 passed, 6 skipped`; `ruff check .` clean; `makemigrations --check` → No changes detected; `manage.py check` clean. Commit = 4 paths (+126/−3). **Plan error found and corrected with proof:** Step 1's `set(Method._meta.get_fields()) - set(Technology._meta.get_fields()) == {id_field}` is unsatisfiable (Django `Field.__eq__/__hash__` use `creation_counter`; the literal failure was captured first) → replaced with class/label/db_table assertions plus a **name-set** difference, which still catches asymmetric drift. Migration backout proven on a DB copy (forward → `content_technology` exists; `migrate content 0032` → unapplied, `content_method`/`content_research_topic` untouched; forward again → recreated). Task 2's test left byte-identical. |
| 4 | Public-key generator | **done** | `1e179f2` + fix `7239e7e` | RED `ModuleNotFoundError: No module named 'apps.atlas.keys'` → GREEN `4 passed`; full suite `955 passed, 6 skipped`; ruff clean; no migration (no models). Commit = 2 new files (+83). **Plan defect found with a brute-force proof:** the specified `"~" not in value.split("~")[0][:1]` clause is a tautology (11,111 strings of length 0–4 probed, 0 counterexamples) — kept, recorded as dead code. **Latent validation weakness found and fixed** (`7239e7e`): the `$`-anchored grammar accepted a trailing newline under both `match` and `fullmatch` (`is_valid_public_key("ab\n") is True`), which would have been inherited by Task 6's `clean()`; the pattern now ends in `\Z` and the validator uses `fullmatch`, with tests asserting both entry points reject `"ab\n"`. Suite after the fix `957 passed, 6 skipped`; keys tests `6 passed`. |
| 5 | Taxonomy models and lifecycle rules | **done** | `f906dae` | RED `ImportError: cannot import name 'models'` → GREEN `11 passed`; full suite `968 passed, 6 skipped`; ruff clean; `makemigrations --check` clean; `manage.py check` 0 issues. Commit = 4 files (+489): `apps/atlas/models.py` (`AtlasNodeType`, `AtlasRelationType`, `SEMANTIC_ROLES`/`VISUAL_ROLES`/`SELF_LOOP_POLICIES`/`CANONICAL_SOURCES`), `apps/atlas/migrations/{__init__.py,0001_initial.py}`, `apps/atlas/tests/test_taxonomy.py`. Backout ladder on a DB **copy**: `migrate atlas 0001` → `atlas zero` (both tables dropped) → `migrate atlas` (recreated), applied 69→70→69→70, auth untouched, worktree `dev.sqlite3` never written. **Plan defects found:** (a) Task 5's Step 1 snippet creates `AtlasVersion`/`AtlasNode`, which do not exist until Task 6 — so its "expect 3 passed" was unsatisfiable and the implementer wrote 11 executable tests instead, proving the guard branches with a monkeypatched stub; (b) Step 4's `--name initial_taxonomy` conflicts with the Files block/commit line — `0001_initial.py` chosen (2-of-3 consistency, Task 6 continues at `0002`). **Spec-driven addition (accepted):** the immutable-key-once-used rule was also wired to `AtlasRelationType` because spec §5.3 requires it and no task assigned it; the guard resolves `AtlasNode`/`AtlasRelation` via `apps.get_model` and auto-activates when they appear. Other notes: `apps/atlas/migrations/__init__.py` was credited to Task 1 by the File Map but never landed — created here; `Meta.ordering` unspecified in places → `["sort_order", "key"]` (Task 5 precedent, determinism); `CANONICAL_SOURCES` name exists as both a `models.py` TextChoices and Task 8's registry dict — flagged to Task 8. |
| 5-cf | **Carry-forward to Task 6** | — | — | Task 6 MUST re-prove with real models: (a) the taxonomy immutable-key guard with an actual `AtlasNode` referencing a type (message verbatim: "A node type key is immutable once a node uses it."), and (b) `ProtectedError` when deleting an in-use type. Both were only provable against a monkeypatched stub at Task 5. Written into Task 6's brief. |
| 6 | Version, node and node-translation models | **done** | `b83de68` (+ fix `b1e55d7`) | RED `ImportError: cannot import name 'AtlasNode'`, then a **second real RED** where the plan's own snippet raised `TransactionManagementError` (fixed with `transaction.atomic()`, the Task 5 bug class). GREEN `6 passed`; `test_taxonomy.py` `16 passed`; full suite `979 passed, 6 skipped` (baseline 968). ruff/`makemigrations --check`/`check` clean. Commit = 5 paths (+595/−44): `models.py` (`VERSION_STATUSES`, `MOBILE_OVERVIEW_PRIORITIES`, `AtlasVersion` + partial unique-active constraint + `layout`/`layout_revision`/`created_from`, `AtlasNode`, `AtlasNodeTranslation`), `migrations/0002_version_node.py`, `tests/factories.py` (**R4 discharged**: `_node_type`, `_relation_type`, `_node`, `_version`), `test_models_version_node.py`, `test_taxonomy.py` (builders now imported). Backout ladder on a DB copy, verified from `django_migrations` rows: 73 tables/69 applied → forward 80/71 → `atlas 0001` **77/70, dropping only `atlas_node`/`atlas_node_translation`/`atlas_version`** → `zero` 73/69 → forward 80/71. **Ledger correction (reviewer):** the *first-ever* atlas forward also adds **20 `auth_permission` rows (244→264)** through `post_migrate`, and `migrate atlas zero` does **not** remove them — so "auth unchanged" means *no auth row was touched by the unapply/reapply cycle*, not that the auth tables are untouched. Owner's `dev.sqlite3` md5 `4e73e231…` unchanged. **All five review-mandated carry-forwards closed with real rows** (`test_node_type_key_is_immutable_once_used` with the verbatim message, `test_in_use_taxonomy_cannot_be_deleted` → `ProtectedError`, `test_retiring_an_in_use_type_is_allowed`, `test_the_in_use_guard_is_armed_for_real_node_rows`, `test_in_use_detection_reads_through_the_base_manager`) — plus a **non-vacuity probe**, independently reproduced by the reviewer: both mutations together → `2 failed, 979 passed, 6 skipped`, failing exactly `test_in_use_detection_reads_through_the_base_manager` and `test_node_key_generator_rejects_a_type_key_containing_the_separator`. (Earlier ledger text said `2 failed, 20 passed` — that was the implementer's test-file-scoped tally; the suite-level number is the one above.) Rulings: `accessible_label_override` added although the Interfaces line omitted it (spec §5.3:270 defines it and the wire carries `accessibleLabel` per §10.2:552 — spec is authority #1; the plan's Interfaces line is the defect); pins implemented as x/y paired **plus** z-requires-x/y (superset of the plan's "all-or-none"); `AtlasNodeTranslation` ordering unspecified → `["node", "locale"]`; node-key `~` rejection lives in `clean()` (the shared `PUBLIC_KEY_RE` must keep permitting `~` for relation keys); `save()` copies `canonical_source` from the node type so `clean()`'s equality rule is satisfiable. |
| 6-cf | **Carry-forwards to Task 7** | — | — | (a) relation-type immutability + `ProtectedError` + retire-in-use with a **real** `AtlasRelation`; (b) `_referencing_model("atlas", "AtlasRelation") is not None` armed assertion; (c) group-key `~` rejection with a test; (d) composed relation keys validated **per segment** (never `is_valid_public_key`, whose 80-char cap is smaller than a legal ~210-char composed key); (e) extend `factories.py` rather than adding a second builders module. Written into Task 7's brief. |
| — | Orchestrator fix | — | `b1e55d7` | `new_node_key` now raises `ValueError` on a type key containing `~` (the LOW finding: `new_node_key("a~b")` minted a node key that every URL codec would read as a relation), with two tests (generated node/group keys never contain `~`; the generator refuses one); `is_valid_public_key`'s docstring corrected — `fullmatch` was already `False` for `"ab\n"` on 3.11/3.12/3.14, only `match` had the hole — and now records that the helper validates a **single segment** (2–80 chars), not composed relation keys. Suite `981 passed, 6 skipped`; keys tests `8 passed`; ruff clean. |
| 6-fix | Task 6 review's required items + R9 in code | **done** | `119b481`, `9271d0e`, orchestrator `611619c` | **A** `test(atlas): cover the node validation branches and fix the version factory` (3 paths, +200/−4): the four `clean()` branches, the `save()` canonical-source copy, the `ValidationError`-raising aliases rule (spec §5.3:271), and the `_version()` collision fixed — RED `3 failed, 11 passed` with the factory bug's real shape captured verbatim (`MultipleObjectsReturned: get() returned more than one AtlasVersion -- it returned 2!`, order-dependent: `_node()` → `_version()` → `_node()`); 8 mutations used to prove the coverage tests are non-vacuous. **B** `fix(atlas): scope public-key uniqueness to the version so clones keep stable keys` (5 paths, +126/−27): RED `IntegrityError: UNIQUE constraint failed: atlas_node.public_key` / `atlas_group.public_key` + a clone regression; migration `0004_version_scoped_public_keys.py` (RemoveConstraint ×2 + AddConstraint ×2 → `atlas_node_version_public_key`, `atlas_group_version_public_key`); the fixer also inverted Task 7's `test_group_public_key_is_unique_across_versions` and corrected a `factories.py` docstring rule I had not listed; under a `('version',)`-only constraint its own within-version assertion passed for the wrong reason, now guarded by a different-key control insert. Gates after B: `1008 passed, 6 skipped`, ruff clean, no drift. **Orchestrator `611619c`** closed the two review findings that were mine to close — see 6-fix2. |
| 6-fix2 | Task 7 review F1+F2 closed | **done** | `611619c` | **F1:** the reported duplicate-name guard did not exist, so it was built for real — `tests/test_test_hygiene.py` AST-scans `tests/**/test_*.py` + `apps/**/tests/**/test_*.py` for shadowed top-level test names, with a non-vacuity assertion that it found ≥10 files. **F2:** `AtlasRelation.clean()` now validates the composed key with `is_valid_relation_public_key`, closing the four-part-key gap a `.update()`-written tilde-bearing endpoint could create. **Both mutation-proved:** injecting a duplicate name into `test_taxonomy.py` failed exactly the guard test (file restored, 0 diff lines), and disabling the composed-key branch failed exactly its test. Suite `1011 passed, 6 skipped` (both `pytest` and `pytest tests/ apps/` invocations), ruff clean, no migration drift. |
| 7 | Relation, relation-translation, group and membership models | **done** | `6737067` | RED `ImportError: cannot import name 'AtlasGroup'/'AtlasRelation'` (2 collection errors) → GREEN `34 passed` across the two files (`14` new + `20` taxonomy; the plan's Step 4 `4 passed` counts only its own snippet's four tests, all present). Full suite `999 passed, 6 skipped` (baseline 981). ruff/`makemigrations --check`/`check` clean. Commit = 5 paths (+792/−12): `models.py`, `migrations/0003_relation_group.py`, `tests/factories.py` (+`_relation`, `_group`, `_group_translation`, `_relation_translation`, `_membership`), `test_models_relation_group.py`, `test_taxonomy.py`. Backout ladder on a DB copy: 69 applied → forward applies 0001–0003 (five new tables) → `atlas 0002` drops only 0003's tables → `zero` → forward; `content`/`auth` applied sets never moved; owner's `dev.sqlite3` md5 `4e73e231…` unchanged. **All five carry-forwards closed (9 named tests):** relation-type rename blocked with `{"key": ["A relation type key is immutable once a relation uses it."]}`, `ProtectedError` on deleting an in-use relation type, `active=False` retirement allowed, guard armed for real relation rows **plus a sensitivity proof** (`_referencing_model("atlas","AtlasRelations") is None` → True), group key never contains `~`, composed 172-char relation key valid per segment while `is_valid_public_key` rejects it (and a bad segment rejected), factories extended with no second module. **7-mutation non-vacuity probe**: each mutation failed exactly its test; `models.py` restored byte-identical. Rulings: `directed` is deliberately **never coerced on save** (a save-level coercion would make Task 9's `DIRECTION_NOT_OVERRIDABLE` unreachable through the ORM and unbuildable in Task 8's fixture) — initialisation from `directed_default` lives in the test builder; `AtlasGroupMembership.clean()` gained a same-version endpoint rule (analogue of the relation rule); `related_name="members"` chosen because the plan's own assertion (`group.members.count() == 2`) counts membership rows; `Meta.ordering` deterministic everywhere. |
| 7-f | **Finding: shadowed test name — mechanism real, claimed guard ABSENT (review F1)** | — | guard to be added (follow-up) | The implementer's real-row test initially reused an existing monkeypatched test's name, so Python silently replaced it: pytest collected 15 definitions and ran 14 (**reproduced independently** by the reviewer — a duplicate silently replaces the original body). Renamed in `6737067`. **However: the reported "AST scan asserting zero duplicate top-level test names" does not exist** — the reviewer found zero `ast.parse`/`FunctionDef` hits anywhere in the commit's tree, the live worktree or `.atlas-evidence/raw/`, so the Task 7 report and this ledger row (as first written) recorded a protection that was not there. Ruling: make the claim true — add a real, collected guard test scanning `tests/**/test_*.py` + `apps/**/tests/**/test_*.py` for duplicate top-level function names (the reviewer's own repo-wide scan over 88 files found zero existing duplicates, so the guard passes today). Until that commit lands, no durable guard exists. Lesson stands and is reinforced: a count that does not match the expected count is a defect signal — and a *claim* about a guard must be verified by finding the guard. |
| 7-g | **Ledger figure corrected** | — | — | A composed relation key can reach **226** chars (`80 + 1 + 64 + 1 + 80`), not the "~210" recorded earlier; the conclusion (never use `is_valid_public_key` on a composed key) is unchanged and now has an executable proof. Tooling note: MSYS `grep -c $'\r'` reports 0 on CRLF files (false negative) — EOL claims must come from Python byte counts. |
| **PD-1** | **Plan D rollback caveat (found while landing R9)** | — | record in Plan D's rollback plan | Reversing migration `0004_version_scoped_public_keys` is only clean on a database holding **no cross-version duplicate keys**: the reverse re-adds global uniqueness, so a clone that reused its source's keys makes the reverse fail with `IntegrityError: UNIQUE constraint failed: new__atlas_node.public_key` (reproduced; it succeeds after the duplicated rows are removed). Plan D's rollback plan must therefore either drop cross-version duplicates before reversing `0004`, or roll back forward-only (restore a pre-cutover snapshot) whenever a live draft shares keys with the active version. |
| 8 | Canonical CMS-record reference strategy | **done** | `4c2071c` | RED `ModuleNotFoundError: No module named 'apps.atlas.canonical'` (2 collection errors) → GREEN `26 passed` (13+13 defs; `--collect-only` confirmed 26). Full suite `1037 passed, 6 skipped` (baseline 1011) on **both** `pytest tests/ apps/` and bare `pytest`. ruff clean; `makemigrations --check` → No changes detected (no migration implied); `manage.py check` clean; worktree `dev.sqlite3` md5 `4e73e231…` unchanged. Commit = 6 paths (+1352/−17): new `apps/atlas/canonical.py` (registry `CANONICAL_SOURCES`, `RESOLVER_FAMILIES`, `SUMMARY_FIELDS`, `resolve_canonical`, `AmbiguousCanonicalRef`, `CanonicalResolution(source_key, family, route_family)`), new `test_canonical.py` + `test_factories.py`, `factories.py` (+610/−6: `seed_pairs` + the R5 bundles), `atlas_preflight.py` (the single import-line change — its local allow-list is gone and it imports the registry), `models.py` (docstring-only collision note). **7/7 mutations caught by exactly their test, restores byte-identical.** Rulings/findings: (a) the plan's `project.summary` snippet is **unsatisfiable** — `Project` has no `summary` field (verbatim `ValueError: … fields do not exist in this model`) → the rule was kept, the snippet adapted; (b) `SUMMARY_FIELDS` kept verbatim with a tolerant lookup plus a guard test; (c) `canonical.family` is the **record-resolver family slug** (`research_topic` → `researchtopic`), asserted against `RESOLVER_FAMILIES`, while the registry *key* travels in `CanonicalResolution.source_key` — two fields, no ambiguity; `route_family = ROUTE_FAMILY_MAP[family]` is imported, never duplicated, and is `None` for `method`/`technology` (no public route in v1 — no route invented); Task 14 still owns `projection.py`/`href`. |
| 8-tool | **Tooling lessons for Tasks 9+ (reuse, do not re-learn)** | — | — | (1) Bare fixture imports trip ruff `F401`+`F811` → list fixture names in the module `__all__` (proven fix, used by both Task 8 test files). (2) Windows text-mode writes rewrite EOLs → write bytes when line endings matter (caught by a sha mismatch and restored LF byte-identically). Both are in the Task 9 brief. |
| 8-h | **Hand-off to Task 10** | — | — | The plan's Task 10 snippet `test_invisible_nodes_and_relations_do_not_participate` is **unsatisfiable** with any fixture that also satisfies its sibling parity test: hiding one one-sided node leaves the other blocking. Fix: hide **both** one-sided nodes in that test (the `atlas_v1` fixture supports it). Must be written into Task 10's brief. |
| 8-fix | Task 8 review's CHANGES REQUIRED items | **done** | `cb63f71` | All five landed with proof. **M1 (summary precedence):** first demonstrated the gap was real — inverting the lookup on the *pristine* tests still gave `1051 passed` — then pinned it (both `short_description`+`description` populated for `method`/`technology`, earlier field wins, plus the earlier-empty fall-through) and proved the pin: inverted order on the final code → the named test FAILS with `"Method short description" vs "Method long description"`, restored byte-identically (sha256 equals the committed blob), green after restore. **M2 (my hygiene guard's coverage gap):** `TEST_GLOBS` now mirrors `testpaths`×`python_files` (`tests/**/test_*.py`, `tests/**/*_test.py`, `apps/**/test_*.py`, `apps/**/*_test.py`), deduped, docstring aligned; demonstrated with a scratch `apps/atlas/test_dup_probe.py` (two identical names) that pytest collects **1** test while the **old** guard passed (`2 passed`) and the **new** guard FAILS with the offending file/name — probe deleted, status back to baseline. **L3:** RED `assert 2 == 3` first, then the `[:2]` slice dropped for a true `count=len(rows)`, with a three-twin test. **L4:** all four `§22` citations corrected to §5.2 + §25; a repo grep for `§22` under `apps/`+`tests/` now returns nothing. **L5:** `factories.py` documents that the two active-version bundles are mutually exclusive in one test, with the `IntegrityError: UNIQUE constraint failed: atlas_version.status` reproduced. Gates: touched files 30 passed, `apps/atlas/tests/` 104 passed, suite `1052 passed, 6 skipped` on both invocations, no migration drift. Commit = 4 paths (+82/−26). |
| 8-fix2 | Orchestrator hygiene fix | **done** | (uncommitted scratch) | `ruff check .` had started failing on **untracked scratch** (`.atlas-evidence/task-09-mutation-probe.py`, E501) — which would have failed every later task's gate for an unrelated reason. Renamed the scratch Python to `.py.txt`; `ruff check .` → All checks passed, suite unchanged at `1052 passed, 6 skipped`. Ruling for the rest of the run: **no `.py` scratch inside the repo** — mutation-probe scripts live as `.py.txt` under `.atlas-evidence/` or in `%TEMP%`. Written into Task 10's brief. |
| 9 | Relation validation rules | **done** | `ded6cf3` | RED `ModuleNotFoundError: No module named 'apps.atlas.validation'` (the plan predicted `cannot import name 'validate_relations'` — same class, module absent) plus `assert 5 == 7` and `AttributeError: 'AtlasV1Fixture' object has no attribute 'relation_type'` on the factory pin test → GREEN `27 passed` (two files), `103 passed` for the atlas package. Full suite `1051 passed, 6 skipped` (baseline 1037) on **both** invocations; ruff/`makemigrations --check`/`check` clean; no migration; all four files LF-only. Commit = 4 paths (+628/−8): new `apps/atlas/validation.py` (`Issue{code,node_key,relation_key,group_key,message_token}`, `ValidationReport(blocking, warnings)` + `blocking_codes()`/`warning_codes()`, `ATLAS_ISSUE_CODES`/`BLOCKING_CODES`/`WARNING_CODES`, `validate_relations(version)`, plus the pure rule layer `relation_rule_issues(relations, *, version_id, allowed_types)` with `AllowedTypes`/`allowed_types_by_relation_type()` preloading pairs in 2 queries), new `test_validation_relations.py` (13 tests: one per code plus clean-fixture control, token/shape, ordering, query-count, invisible-relation scope, code-set consistency), `factories.py` (+85/−4: `atlas_v1` gained `relation_type`, `bad_relation` (research-area→identity against the type's pair), `direction_relation` (`related-to` authored `directed=True`); `research-focus`'s allowed pair seeded to spec §6.2's identity→research-area), `test_factories.py` (+42/−4, re-pinned 5→7 relations / 1→2 types + a new offender-surface test). **7/7 mutation probe**, each caught by exactly its test: M1 pair rule disabled → 3 tests; M2 inactive disabled; M3 self-loop comparison inverted; M4 mirror identity dropped; M5 override clause dropped; M6 dangling rule disabled; M7 per-relation re-query → the query-count test. Scope decisions: `validate_relations` returns the **flat** list — the blocking/warning split and `to_dict` stay Task 12's aggregator (it added `blocking_codes()` early because Task 10's snippet calls it); **all** relations are judged, visible or not, pinned by a test (Task 10's `DANGLING_NODE_HIDDEN_RELATION` is unaffected); the exact-triple duplicate is DB-unreachable through the ORM (`atlas_relation_unique_version_pair_rel`), so it is proven on the pure layer with detached rows while the mirrored-undirected half is proven end-to-end. |
| 9-cf | **Carry-forward to Task 12 (spec code-list gap)** | — | — | `DIRECTION_NOT_OVERRIDABLE` is **not** in spec §20.1's blocking list. Task 12's step "freeze the vocabulary equal to §20.1 + §20.2 exactly" must therefore read **§20.1 + §20.2 + `DIRECTION_NOT_OVERRIDABLE`**, and the final report must flag the spec gap (a real blocker code with no spec row) for the owner. Written into Task 12's brief. |
| 9-fix | Task 9 review MEDIUM-1 + LOW-2/3 | **done** | `5edd69a` | **MEDIUM fixed with TDD**: RED was exactly the asymmetry — `1 failed, 14 passed`, only the `undirected-authored-ascending` parameter failing with `KeyError: 'DUPLICATE_RELATION'` while the descending parameter already passed. Fix: an undirected row in `_identities` now answers to **both** `(min,t,max)` and `(max,t,min)`, with `_duplicate_issues` de-duplicating a shared key. Both orientations are proven **load-bearing** by mutation (M1 dropping `(max,t,min)` fails only the ascending parameter; M2 dropping `(min,t,max)` fails only the descending one), and M5 (making *directed* rows claim both orientations) is killed by the new directed-legality test plus four existing tests, so the fix did not over-reach. Coverage gaps closed: a foreign-**target** dangling test (M3 comparing only `source.version_id` fails exactly it — proving the earlier blind spot) and a keyless detached-row test for Plan B's bulk-PUT shape (M4 disabling `if key is None:` fails exactly it, with the crash `AtlasRelation has no relation_type.` captured). Gates: `test_validation_relations.py` 18 passed, `test_validation_hierarchy_locale.py` 22 passed, atlas package 132 passed, suite **1080 passed, 6 skipped** on both invocations (baseline 1075), ruff clean, no drift, both paths LF-only. Commit = 2 files (+162/−5). |
| 9-note | **Note for Tasks 12/15 (issue-count nuance, not a defect)** | — | — | When an undirected row and a *reversed directed* row carry **different** key spellings, `DUPLICATE_RELATION` reports **both** spellings (one issue per shared key) rather than one merged issue; when the spellings coincide (the descending case) it reports once. The detection verdict is now authoring-order independent either way, which is the property §5.3's sorted key demands. Any consumer that counts issues rather than de-duplicating by relation pair (Task 12's report, Task 15's diagnostics) must treat these as one conflict. |
| 10 | Hierarchy DAG, locale parity and taxonomy lifecycle validation | **done** | `f031b2a` | RED `ImportError: cannot import name 'validate_canonical_refs' from 'apps.atlas.validation'` (the plan predicted `validate_hierarchy` — same class) → GREEN `64 passed` (touched set), `127 passed` for `apps/atlas/tests/`, suite **`1075 passed, 6 skipped`** (baseline 1052) on both invocations; ruff/`makemigrations --check`/`check` clean; no migration; all five files LF-only. Commit = 5 paths (+1274/−15): `validation.py` (+382/−12: `validate_hierarchy` + pure `hierarchy_rule_issues`, `validate_visibility`, `validate_locale_projection`, `validate_taxonomy`, `validate_canonical_refs`; eight new blocking codes + tokens; `WARNING_CODES` still `()`), new `test_validation_hierarchy_locale.py` (+568, 22 tests), `factories.py` (+255: the `atlas_dag` family — fixed ascending `DAG_KEYS`, a `parent-of` type, a directed `relates-to` type, structural nodes via per-locale overrides, `close_cycle()`/`add_related_cycle()`), `test_factories.py` (+55/−1), `test_validation_relations.py` (+14/−2: local `MESSAGE_TOKENS` extended additively so its `set(BLOCKING_CODES) == set(MESSAGE_TOKENS)` assertion stays true). **Mutation probe 15/15**, each caught by exactly its named test — including M1 "parity ignores visibility", M3 "cycle detection off", M4 "second parent reported" (the multi-parent legality), M7 "pk order instead of `public_key`" (determinism), M8/M13 (dangling + hidden-relation visibility), M14 "`hierarchy_role` filter dropped" (general cycles stay legal). **Three of this task's snippets were unsatisfiable as written** — `8-h` plus two more, one of them **the Phase 0 correction-1 parity test, whose overrides targeted the locale that already resolved**; all three corrected with intent preserved (both one-sided nodes hidden; assertion scoped to the node under test; overrides written on the **missing** locale with the plan's wrong-locale calls kept as the no-fallback proof), and the plan section now carries an *Execution amendments* block naming them. Additional execution facts: the task ships **five** validator groups rather than the plan's four — `DANGLING_NODE_HIDDEN_RELATION` gets its own `validate_visibility` because grouping it under a plan name would hide a blocker, so **Task 12 must aggregate six functions** — and `atlas_dag` (owned by no task) lives in the single builders module per R4/R5/R10, making the commit five paths rather than the plan's two. Module-docstring rulings: parity is visible-scoped and blocking in both directions; canonical refs follow the projection; taxonomy judges every node; the group copy gate judges every group regardless of `active`; `CANONICAL_SOURCE_UNPUBLISHED` (record-level) is distinct from `MISSING_LOCALE_PROJECTION` (locale-level); **`MULTIPLE_PARENTS_ALLOWED` does not exist** (multi-parent needs no code). |
| 10-fix | Task 10 review F1 (MEDIUM) + F3 + F2 | **done (orphaned by a provider outage — orchestrator committed)** | `3b35f16` | **Process note:** the fixer ran 52 API calls over ~15 h and then died on *"Hermes can't reach the model provider"* **before committing**, so no implementer self-report or mutation-evidence file exists for this work. The orchestrator found the two files complete in the working tree (`validation.py`, `test_validation_hierarchy_locale.py`, +352/−69), confirmed the three fixes were present, and verified the tree before committing: 48 passed on the two Task 10 files, suite **1100 passed, 6 skipped** on both invocations, `ruff check .` clean, `No changes detected`, `manage.py check` clean, and no tracked modifications left over. **F1** was fixed with a *different and arguably stronger* design than the reviewer suggested: instead of discarding only the single-relation cycle, the walk became `_witness_walk(arcs, origin, goal, relation_pk)` — for each relation and each of its directions it looks for a witness path that **excludes that relation**, and the arc sort now keys on `(public_key, arc[1])` with a comment arguing no verdict can turn on the tiebreak. **F2** took the stronger option too: the traversal now reports the `public_key`-first member of the cycle, making the docstring's claim **true** rather than softening it. **F3** resolves per missing locale so an ambiguity in one locale can no longer mask the other locale's missing projection. New tests: `test_a_cycle_closed_through_an_undirected_row_is_reported` (parametrised over the F1 shapes), `test_an_undirected_row_is_walkable_against_its_authored_order`, **`test_the_hierarchy_verdict_does_not_follow_relation_row_order`** — which inserts each shape in **both** orders and compares the two verdicts of one logical graph, i.e. exactly ruling **R11**'s falsification form rather than a `public_key`-order assertion — `test_a_cycle_is_reported_at_its_first_node_in_public_key_order`, and `test_an_ambiguous_locale_does_not_mask_the_other_locale_s_missing_projection`. Because the commit is orphaned, a **dedicated falsification review** was dispatched over it (frozen `3b35f16`) with an explicit instruction to assume an interrupted-probe scar may be present: it rebuilds the reported shapes in mirrored orders, hunts for false positives in the new walk formulation (self-loop, two-undirected 2-cycle, mixed triangle, cycle behind a hidden node, parallel arcs, a node reachable by two paths where one is the excluded relation), mutation-tests the new logic, counts removed `assert` lines against `f031b2a`, and scans for leftover probe artefacts. |
| 11 | Deterministic layout engine | **done** | `11c2579` | RED `ModuleNotFoundError: No module named 'apps.atlas.layout'` (narrow — `factories.py` imports the module lazily, so only the new file failed) → GREEN `12 passed`, `144 passed` for the package; suite **`1092 passed, 6 skipped`** (baseline 1080) on both invocations; ruff/`makemigrations --check`/`check` clean; no migration; all four files LF-only; worktree `dev.sqlite3` sha256 `940d6f74…` unchanged. Commit = 4 paths (+1542/−4): new `apps/atlas/layout.py` (580 L — `LAYOUT_CONSTANTS` (the plan's 13 numbers), `LAYOUT_BLUEPRINT` as an **ordered** tuple of `LayoutStage` that *is* the execution order, the nine spec-§12.2 stages, `hierarchy_depths`, `compute_layout`, and `apply_layout` as the only ORM adapter), new `test_layout.py` (431 L, 12 tests: the plan's four snippets in intent + frozen constants, fixture composition, longest-path depths, storage/quantisation, revision, recomputation stability, invisibility, §19.2 budget), `factories.py` (+534: the `atlas_scale_fixture` family — 72 nodes/136 relations/6 groups/36 memberships/4 pins/3 levels with fixed keys, plus `version(visible_nodes=80, relations=150)` for the budget shape; no canonical rows because the engine reads none), `pyproject.toml` (+1: registers the plan's `slow` marker). **Determinism proof (the load-bearing evidence, with one corrected premise).** Two separate interpreter runs produced identical canonical JSON — `sha256 a31ec1fcb23deb51fda7d4cdcd4940e055f625c5e320edf73e3bfc9567a5ef26` — 72 nodes/136 relations/6 groups, pins honoured verbatim (`identity-00000001 (0.0, 0.0, 0.0)`, areas `(40.0,25.0,·) (-40.0,25.0,·) (0.0,-45.0,·)` at z = their depth layer); in-test, reversed arrival order is byte-identical and a re-apply with unchanged rows moves zero coordinates. **Correction (Task 11 review F4):** the original rationale "two fresh test databases, therefore different pks" is **false** — both fresh sqlite test DBs assigned the same pks 1…72. The conclusion still holds, and the reviewer re-established it properly: the digest reproduces across 2 processes, 6 `PYTHONHASHSEED` values, and a **deliberately pk-shifted database** (nodes at pks 301–372), plus reversed and shuffled arrival orders, groups as shuffled pairs, relations presented as a `set`, a second version with disjoint pks in one DB, duplicate rows, and a hidden node in the input. Cross-OS/libm divergence in `math.cos`/`sin` at the third decimal remains unverifiable on this host (residual, low). **13/13 mutation probe**, each caught exactly as claimed (arrival order, 6-decimal rounding, pins ignored, no revision bump, stored keys `pk-<pk>`, blueprint stages swapped, last-parent-wins depth, hidden nodes laid out, `id()` ordering, composition count, 20× relaxation iterations, `z = 0`, `r_min`); the **first** probe run had three MISMATCHes and the dispositions are the interesting part — two mutations were re-anchored to be rule-pure, and M10 exposed a **real fixture gap** (the requested size was not honoured against the composition as written), now fixed via `delta = visible_nodes - sum(counts.values())`. Documented reinterpretations: spec §12.2 step 3's "`y = 0` plane" is **unsatisfiable** against the plan's own test because step 6 overwrites `z` (no other reading survives the snippet); step 8's "one more relaxation pass" is implemented as the full fixed-iteration relaxation with unpinned **movers**, proven by `_pin_separation`; `groups` is a membership mapping `{group key: member node keys}` (AtlasGroup carries no members) which keeps `compute_layout` query-free for §12.4's centroid rule; "mean inter-root spacing" is defined in code as the mean distance between consecutive roots in `public_key` order. |
| 11-note | **Spec-precision item (owner)** | — | — | Spec §12.2 step 3 says seeds land on a "`y = 0` plane" while step 6 overwrites `z`; the implementation follows the reading that survives the plan's own test (seeds in the x/y plane, z from step 6). If the owner wants a literal y-plane seed, §12.2 needs an explicit ordering statement — otherwise the text and the code will keep disagreeing. |
| fix10-b | The fix-10 review's four items (honest wording, self-loop, tie permutation, witness direction) | **done (orphaned by an outage — orchestrator committed)** | `02d86b1` | **Second consecutive orphan.** The implementer ran 52 API calls over ~3.5 h, reported the gates as green, and died **before committing** — and its final message was a *narration of intent* (\"then stage exact paths and commit…\") rather than a report, so nothing it said could be treated as evidence. The orchestrator applied the recorded orphan protocol: (1) inspected the residue — two modified files, HEAD still `af3ba7e`, no `fix10-b` commit anywhere in the log; (2) verified the work is *present*: package 160 passed, suite **1108 passed, 6 skipped** on both invocations (baseline 1103 + 5 new tests), ruff clean, `No changes detected`, `manage.py check` clean; (3) located all four items in the code — the honest docstring now reads \"``HIERARCHY_CYCLE`` per **named** node, at the first node in ``public_key`` order of the cycle its witness closes\"; `test_a_directed_self_loop_is_a_cycle_of_one_relation` (referencing M3 in its own docstring), `test_a_tie_bearing_verdict_does_not_follow_relation_row_order`, `test_a_name_that_only_the_second_walk_direction_finds_is_reported`, and — the item I explicitly recommended — `test_a_cycle_whose_first_node_never_wins_a_witness_goes_unnamed`, which encodes the 4-node/6-row counterexample so the claim cannot drift back; (4) scar-hunted the two changed files (no `print`/`pdb`/`breakpoint`/`if False`/`noqa`/TODO), proved **0 removed `assert` lines** against `af3ba7e` (only 3 comment/doc lines removed) and both files LF-only; (5) committed with the intended message (2 paths, +184/−3; `validation.py` **+8/−1** = one docstring hunk, the +3 deletions being the commit-wide count). Because no implementer mutation probe was ever reported for it, a **scar-hunting review** was then dispatched over the commit, charged with reproducing the counterexample, mutation-testing each *new* test (self-loop short-circuit, one-direction walk, arrival-order sort, origin/goal reporting) — where **any survivor is the finding** — reading every changed line of `validation.py` for an embedded mutation, and proving the \"wording-only\" claim by behaviour equivalence against `3b35f16`. |
| 11-fix | Task 11 review F1 (MEDIUM) + F2 (MEDIUM) + F3 + F4/F5/F6 | **done** | `af3ba7e` | Both MEDIUM REDs reproduced exactly and verbatim before the fix: **F1** `KeyError: 'research-area-00000001'` from a generator, a `filter(...)` and both (the drained one-shot iterator), fixed by materialising `relations = tuple(relations)` once in `_prepare` and building the arcs once into `_Run.arcs` (stage 1 reuses them); **F2** `AssertionError: no unpinned neighbour may be left inside a pin` / `assert -5.631856711361035 >= 0.0` — the reviewer's own 5.632 — fixed by a geometric `_expel_from_pins` pass appended to `_stage_final_collision` (an unpinned mover inside a pin is set exactly on the clearance circle plus one quantisation unit so step 9's rounding cannot steal it; pins and movers in `public_key` order; a coincident mover's ray is a golden-angle turn of its ordered key; the loop repeats until no displacement, ≤ pins+1 rounds). Post-fix the counterexample's tightest pair is **+0.000645**, the pinned coordinate is untouched, and **exactly one of 72 coordinates moved**; a second relax→expel cycle was measured to change nothing (the post-expulsion state is already a fixed point). **F3** the cyclic-input `KeyError` is now a documented deferred fallback in the module docstring, `_depth_by_arcs`, `_stage_seed_placement` and `compute_layout`. **F4** the probe is re-recorded with a `--shift` mode that spends 300 junk pks first (nodes land at 301–372), and the false "fresh DB ⇒ different pks" rationale is removed from both the probe and the evidence file. **F5** the marker claim was reproduced (pytest 9.0.3 warns and passes; `--strict-markers` errors) and the justification reworded with the registration kept. **F6** the unread joins and `_Run.nodes`/`_Run.relations` dropped. Gates: `test_layout.py` 15 passed, atlas package 155 passed, suite **1103 passed, 6 skipped** on both invocations, ruff clean, `No changes detected`, both files LF-only. Commit = 2 files (+260/−24). Mutation probe: M1 un-materialise → F1 test only; M2 remove expulsion → F2 test only; M3 remove the cycle fallback → F3 test only; M4 drop the clearance guard → F2 test; M5 constant tie-break ray → no test fails and the mutant is **byte-identical** for the counterexample, recorded as *unobservable/equivalent* rather than as a caught defect. Determinism: the digest `a31ec1fc…` is identical across all 12 re-recorded runs (2 process batches × seeds 0/1/7/99/31337/424242 × {plain, `--shift`}) **and equal to the pre-fix digest**, so the fixes move no fixture coordinate; a `pk % 7`-dependent control engine produces plain `7474862e…` vs shifted `9d519542…`, proving the shift is a real discriminator. |
| 11-fix-note | **CORRECTED (see ruling R12 below and the `af3ba7e` review): §12.2 step 8 is GLOBAL, so the pass is incomplete and must not worsen any pair** | — | — | On the reviewer's adversarial input the residual **unpinned↔unpinned** overlap reads **−9.095 after the fix** versus **−2.976 before**: expulsion moved a mover out of a pin and into another mover. The implementer disclosed this rather than hiding it, and the pass is deliberately pin-scoped per the F2 ruling's wording (§12.2 step 8 exists "so a pin cannot leave an unpinned neighbour overlapping it"). If §12.2's collision rule is meant to cover **all** pairs, this needs a follow-up pass that iterates collisions rather than pins only; the falsification review dispatched over `af3ba7e` is asked to rule on exactly that, quoting the spec text. |
| 12-note | **Aggregator obligations inherited from the fix-10 review** | — | — | (a) an `Issue` carries **no locale**, so a node missing both locales emits two indistinguishable `MISSING_LOCALE_PROJECTION` rows — the aggregator must de-duplicate per node+code; (b) one doc sentence should settle that **two undirected hierarchy rows on one pair ARE reported as a cycle**, since §5.6's "multiple relation types between the same pair" reads as if they were not. |
| 12 | Validation report + payload contract check | pending | — | freezes the issue-code vocabulary as **§20.1 + §20.2 + `DIRECTION_NOT_OVERRIDABLE`** (row `9-cf`); must aggregate **six** validator functions (Task 10 shipped `validate_visibility` too); must de-duplicate `DUPLICATE_RELATION` by relation pair (`9-note`); and should resolve canonicals **once per node** and share the result with the locale validators (review measured 133/333/413 queries at 30/80/100 nodes — TODO Task 12) |
| 13 | Clone and publish services | pending | — | (a) the activation **must be archive-then-activate** under `select_for_update`: the reviewer reproduced `activate-then-archive → IntegrityError ("atlas_version.status")` against the partial unique-active constraint (Task 6 review, design judgement 1); (b) the clone carries its source's keys, which is only legal because of ruling **R9** (per-version uniqueness) — the clone regression test exists since `9271d0e`; (c) Plan D's rollback caveat `PD-1` applies to this migration set |
| 14 | Locale projection and payload assembly | pending | — | consumes Task 8's `CanonicalResolution` (`source_key`/`family`/`route_family`) and owns `projection.py` + `href`; the payload's `canonical.family` is the **record-resolver slug** (`researchtopic`), not the registry key; `route_family` is `None` for `method`/`technology` (no public route in v1) |
| 15 | Public endpoint, draft invisibility, fail-closed behaviour | pending | — | `preview_tokens.py` + both public routes |
| 16 | ETag, If-None-Match and Cache-Control | pending | — | — |
| 17 | Wire contract notes document | pending | — | keeps the preflight format (R7) |
| 18 | Seed-free test fixtures for scale | pending | — | 72-node/136-relation fixture + dump |
| 19 | OpenAPI export, re-pin and consumer type regeneration | pending | — | cross-repo, per-repo commits (R8) |
| 20 | Plan A acceptance run | pending | — | 20 plan-completion proofs |

## 5. Pre-existing dirty entries in the main checkout (untouched — evidence)

Captured at baseline with `git status --porcelain`; must remain byte-identical at completion:

```
 M .github/copilot-instructions.md
 M .gitignore
 M AGENTS.md
?? .adal/            ?? .gemini/        ?? .mcp.json
?? .claude/          ?? .grok/          ?? .windsurf/
?? .cursor/hooks.json ?? .ignore        ?? GEMINI.md
?? .cursor/hooks/    ?? .kiro/          ?? opencode.json
?? .cursor/mcp.json  ?? .cursor/rules/graft.mdc
```

= 3 modified tracked files (all agent-tooling/config, none in `apps/`, `config/` or `tests/`) + 14 untracked agent-tooling entries. The same list is re-captured and compared at Task 20; any change is a finding, not a cleanup.

## 6. Review findings / fixes

### Task 1 review (combined spec-compliance + code-quality pass; git range `6c0d349..8f6ff2c`)

Verdict: **APPROVED** — spec compliance PASS, code quality APPROVE. The reviewer independently reproduced every claim (RED/`Unknown command`, `2 passed`, `948 passed, 6 skipped`, exactly 8 paths (+186), read-only proven by DB md5 unchanged, `--json` omitted writes nothing, exit 1 reproduced with a seeded unpaired record, `manage.py check` clean, ruff clean). Nothing Critical, nothing Important in the commit itself.

Findings ACTIONED (orchestrator-applied as two narrow commits, since both are one-line-scale):

| Finding | Action | Commit |
|---|---|---|
| Report entry lacked the `published` key the plan's Interfaces line lists | Added `"published": True` (rows are filtered on `status="published"`, so it documents intent) and aligned the contract doc | `9f5307b` |
| Key-vocabulary conflict: the command and its doc fixed `--models` at `Model._meta.model_name` (`researchtopic`) while Task 8's `CANONICAL_SOURCES` uses `research_topic`, so Task 8's "single import-line change" was not achievable | Report/CLI now use the canonical-source spelling (`research_topic`), with the Django model name reported separately as `modelName`; the payload's `canonical.family` family-slug question is explicitly delegated to Task 8/14 with the `record_resolver` reference | `9f5307b` |
| **CI never runs the Atlas tests**: `pyproject.toml` had `testpaths = ["tests"]`, so `uv run pytest` (`.github/workflows/ci.yml:41`) collected 952 tests and silently skipped every `apps/atlas/tests/*` file — the whole Plan A test suite would have been invisible to the real gate | `testpaths = ["tests", "apps"]`; bare `pytest` now collects and runs them (`949 passed` at the time, `951 passed, 6 skipped` after Task 3's in-flight work) | `fadb21f` |
| Minor: no `help=` on the options; unknown `--models` key raised a raw `KeyError` | Both options documented; unknown key now raises `CommandError: unknown canonical source 'x'; known keys: profile, research_topic` (verified by running it) | `9f5307b` |
| Minor: `ATLAS-PAYLOAD-CONTRACT.md`'s `blocking` example was a placeholder object | Replaced with a real, copyable shape description | `9f5307b` |
| Observation: the preflight does not flag two same-locale rows sharing one `translation_key` | Recorded, not fixed here — Task 8's `AmbiguousCanonicalRef` hard-fails on it; noted for Plan D's prerequisite doc | ledger only |

Process lesson recorded: **never run the full suite in the worktree while an implementer is mid-task.** One bare-`pytest` run reported `1 failed, 950 passed` while Task 3's implementer was writing `apps/content/models.py` before its migration existed; the same command run twice on a settled tree reported `951 passed, 6 skipped`. Ruling: implementers are **serialized** (one writer in the worktree at a time) and reviews/verification run against a frozen commit (`git archive <sha>`) when anything else may be writing.

### Tasks 2 + 3 review (combined pass; frozen-tree technique — reviewed against `git archive 587168e` while Task 4 wrote)

Verdict: **APPROVED** — Task 2 PASS, Task 3 PASS, code quality APPROVE. Independently reproduced: every touch path, the RED outputs, `1 passed`/`3 passed`, suite `949`/`951` at each commit, ruff clean, `makemigrations --check` clean, migration `0032`/`0033` regenerate byte-identically (no hand-editing), `manage.py check` 0 issues, Task 2's test byte-identical across Task 3's edit, the worktree `dev.sqlite3` untouched (sha256 `940d6f74…`, no 0032/0033 rows), and a **fuller migration backout ladder** (forward 0033 → reverse → reverse 0032 → forward) with only the two new tables appearing/disappearing.

Reviewer's correction of the implementer's mechanism claim (recorded as knowledge for later tasks): Django `Field.__eq__` compares **`creation_counter` *and* the owning `model`** (`django/db/models/fields/__init__.py` l.677–681), and auto-created pks use a separate descending `auto_creation_counter` (l.128–131/236–239). Mixin-cloned fields therefore **share** counters across models — the `model` component is what breaks cross-model field equality. The Task 3 correction (compare **names**) stands either way, and its drift detection was proven by mutation (Method-only extra field ⇒ FAIL). The plan's `"Expected: 2 passed"` at Task 3 was internally inconsistent (its own Step 1 adds two functions to one ⇒ 3).

Open minors (no action required; address only when those files are next touched):
  1. The comment in `test_technology_and_method_are_distinct_models` states the `__eq__` mechanism incompletely (omits the `model` comparison).
  2. The name-difference assertion is one-directional (catches Method-side drift only); `names(M) == names(T)` would catch both.
  3. `MethodAdmin`/`TechnologyAdmin` are identical bodies including the docstring — a shared base would avoid divergence.
UNVERIFIED (documentation gap, not a code gap): no Task 2 backout record exists in `.atlas-evidence/`; the reviewer verified migration `0032`'s reverse functionally instead. Rule for the rest of Plan A: every task that touches a migration writes its evidence file.

### Tasks 4 + 5 review (frozen `f906dae`; reviewed while Task 6 wrote)

Verdict: **APPROVED** — Task 4 PASS, `\Z` fix PASS (correct and sufficient for every caller), Task 5 PASS, code quality APPROVE. Independently reproduced by the reviewer: both RED outputs, `4 passed`/`11 passed`, suite `968 passed, 6 skipped`, ruff/`makemigrations --check`/`check` clean, migration `0001_initial.py` content-identical when regenerated (`.gitattributes` forces `eol=lf`), the full backout ladder on its own DB copy (auth/contenttypes/sessions rows identical), the worktree `dev.sqlite3` sha256 `940d6f74…` untouched, and — beyond the implementer's stub — the guard proven against registered stand-in models (rename blocked with the exact message, delete → `ProtectedError`, `active=False` retirement allowed).

Break-attempts on the `\Z` fix (all rejected): `\r`, `\t`, `\r\n`, NUL, spaces, Arabic-Indic/Devanagari/fullwidth/mathematical digits, fullwidth `ａ`, Cyrillic, zero-width space, 1M-char strings (<1 ms, no backtracking); 2–80 bounds hold. The tautology probe was widened to 211,111 inputs with 0 counterexamples.

**Correction to my own fix's justification (fixed after Task 6):** `fullmatch` was *already* `False` for `"ab\n"` on 3.11/3.12/3.14 — only `match` had the hole. The fix remains necessary and correct (plan line 580 requires raw `PUBLIC_KEY_RE.match` callers in `clean()`), but the comment and commit message overstated the failure mode; the comment was amended for accuracy.

Reviewer's key judgement calls, all accepted: (1) `\Z` + `fullmatch` + anchored raw pattern is the right shape, not belt-and-braces; (2) dropping the tautological `~` clause loses nothing (the plan's own tests require composed `~` keys to pass); (3) Task 5 wiring immutability to `AtlasRelationType` is a **correct spec reading, not scope creep** (spec line 214 says so, relation keys are composed from the type key, and a rename would re-key every relation URL); (4) `_referencing_model()` auto-activates correctly with no import cycle and is re-evaluated per save.

REQUIRED follow-up (steered into Task 6, since the plan assigns it nowhere else): real-path tests for rename-in-use → `ValidationError` (verbatim message), delete-in-use → `ProtectedError`, retire-in-use → allowed; an assertion that `_referencing_model("atlas", "AtlasNode") is not None` once the models exist (a typo would otherwise silently disable the rule); `~` rejected in node/group `public_key`; `_referencing_rows_exist` switched to `_base_manager`.

LOW findings recorded (not blocking; act only where a later task touches the code):
  - `save()`-level guard is bypassable by `QuerySet.update()` — Tasks 9/10/18 must not treat taxonomy keys as tamper-proof.
  - `new_node_key("a~b")` produces a tilde-bearing node key (generator hardening queued by the orchestrator); `is_valid_public_key`'s 80-char cap is *smaller* than a legal composed relation key can be (~210 max), so Tasks 15/17 must validate relation keys per segment, never with `is_valid_public_key`.
  - `validate_taxonomy_key(123)` passes (str coercion) and its message quotes `^[a-z0-9-]+$` while the pattern is `\Z`-anchored.
  - `ruff format --check` would reformat new files; the repo's gate is `ruff check .` only (145/254 files unformatted repo-wide) — no action.

### Task 7 review (frozen `6737067`; reviewed while the R9 fixer wrote)

Verdict: **CHANGES REQUIRED** — spec compliance **PASS**, code quality **APPROVE WITH FINDINGS**. Everything load-bearing reproduced: both RED imports, `14`/`20`/`34` passing, suite `999 passed, 6 skipped`, ruff/`makemigrations --check`/`check` clean, the 5-path commit (+792/−12), the DB backout ladder (content last migration `0031_pu_publication_snapshots` and auth last `0012_…` unmoved at every step, worktree md5 unchanged), the 9 carry-forward tests, and the 7-mutation probe (7/7 caught by exactly their stated test, controls green, restore byte-identical).

Findings and dispositions:
  - **F1 — Medium, blocking; ACTIONED.** The claimed "AST scan asserting zero duplicate top-level test names" **does not exist** (zero `ast.parse`/`FunctionDef` hits in the commit tree, the live worktree and the evidence dir), so the Task 7 report — and this ledger, which I had written from it — recorded a protection that was not there. The *mechanism* is real and was reproduced: a file with 15 top-level `def test_` runs 14, because the duplicate silently replaces the original body; `ruff` flags it as `F811`. Disposition: build the guard for real as a collected test over `tests/**/test_*.py` + `apps/**/tests/**/test_*.py` (the reviewer's repo-wide scan of 88 files found zero existing duplicates, so it passes today) and keep the ledger's corrected wording.
  - **F2 — Low; ACTIONED with F1.** `AtlasRelation.clean()` re-implements per-segment validation instead of calling `is_valid_relation_public_key`, so a composed key with **three** `~` passes `clean()` while the helper rejects it. One-line fix + test.
  - **F3 — Low; recorded.** `related_name="members"` yields *membership* rows (the plan's own assertion `group.members.count() == 2` is honestly satisfiable and its value is correct because `(group, node)` is unique), but the name will read as "nodes" to Plan B/C; keep it (plan-forced) and note it for the admin/frontend task.
  - **F4 — Low; recorded.** `directed = BooleanField(default=True)` is applied on omission even for `directed_default=False` types, so a mis-defaulting `create()` is only caught by Task 9's `DIRECTION_NOT_OVERRIDABLE` when `overridable_direction` is false. Deliberate (a `save()` coercion would make Task 9's rule ORM-unreachable), but a footgun worth a comment.
  - **F5 — Low, cross-task, unowned; recorded for the final report.** `weight`/`importance` ranges (0–100) and node/group `public_key` immutability-once-used are enforced by **no** task (the taxonomy keys have the guard; node/group keys do not). Also `public_key` immutability at the DB level, and the `R9` per-version scoping for `AtlasGroup`, are pending — the group half lands with the R9 commit.
  - Reviewer's judgement call accepted: the `_meta.get_fields()` assertion is narrow but meaningful (it fails if a stray stored `public_key` column is ever added).
  - UNVERIFIED (no artifact): the original 33-vs-34 run; "staged by explicit path" (only the committed file set is observable, and it is exactly the five expected paths).

### Task 8 + orchestrator `611619c` review (frozen `4c2071c`)

Verdict: **CHANGES REQUIRED** — spec compliance **PASS** on every probed rule, code quality with two MEDIUM items. Reproduced independently: the exact RED string (by deleting `canonical.py` in a frozen copy), `26 passed` (13+13 collected), `58 passed` on the touched-file set, `1037 passed, 6 skipped` on both pytest invocations, ruff/`makemigrations --check`/`check` clean, both commit path/numstat sets, the full R5 fixture surface, `Project` genuinely lacking a `summary` field (so the plan snippet is unsatisfiable), the `__all__` fix (without it: 1×F401 + 9×F811) and the byte-mode EOL fix (text-mode turned a 0-CRLF file into 3). Falsification probes confirmed: exact-locale resolution both directions, `objects.public()` gating (null/future/draft → `None`), ambiguity detected (2 published twins ⇒ `AmbiguousCanonicalRef`; 1 published + 1 draft resolves), closed ordered allow-list, `family` == model name vs `source_key` == registry key with `route_family` imported from the real map, `route_family is None` for `method`/`technology`, and legal 226-char keys accepted while four-part keys are rejected.

Findings and dispositions (all queued behind Task 9's writer):
  - **MEDIUM-1 — ACTIONED.** The summary-precedence rule is **not pinned by any test**, so the commit's "7/7 mutations caught" label overstates it: the labelled mutation actually tested "only the last entry counts". A true inversion (reverse iteration / keep the last populated field) survives the suite. Fix: assert precedence with **both** fields populated (method/technology) expecting the earlier field, so the order is genuinely pinned.
  - **MEDIUM-2 — ACTIONED (gap in my own guard).** `tests/test_test_hygiene.py` scans `tests/**` and `apps/**/tests/**`, but pytest also collects `apps/**/test_*.py` directly (spec `testpaths = ["tests","apps"]`, `python_files = ["test_*.py","*_test.py"]`): an `apps/atlas/test_dup.py` with two identical names is collected while the guard stays green. Fix: add `apps/**/test_*.py` and `**/*_test.py`, or derive the file list from pytest collection. Its docstring's "every test file" claim must match the code.
  - **LOW-3 — ACTIONED.** `AmbiguousCanonicalRef.count` is capped by a `[:2]` slice (3 published twins report 2).
  - **LOW-4 — ACTIONED, verified by the orchestrator.** Four citations point at "spec §22" for the method/technology no-public-route fact; §22 is *Testing strategy* (line 1197). The fact is **§5.2 line 154** and **§25 line 1333** (*Explicit deferred features*, line 1322). Both citations corrected.
  - **LOW-5 — recorded.** `atlas_active_version` + `atlas_two_versions` in one test raises `IntegrityError: UNIQUE constraint failed: atlas_version.status` at fixture setup (the spec-correct constraint, but undocumented in `factories.py`).
  - **INFO-6/7 — recorded.** `canonical.py`'s "honest outcomes" list mentions the override although the resolver never inspects overrides (the override is applied by Task 10); and Task 8's probe scripts hardcode the live worktree and mutate tracked files in place, so they must not be re-run while another task is writing.

### Task 9 review (frozen `ded6cf3`)

Verdict: **CHANGES REQUIRED** — spec compliance PASS except one named exception. Reproduced independently: the exact RED strings, `27 passed` (each file also alone: 13/14, `--collect-only` 27, no shadowed names), atlas package `103 passed`, suite `1051 passed, 6 skipped` on both invocations, ruff/`makemigrations --check`/`check` clean, no migration, exactly 4 paths +628/−8, LF-only by byte count, `validation.py` md5 match, and the 7/7 mutation probe with each mutation failing exactly its stated test. All six codes falsified both ways (30/30 probe checks), interfaces verbatim against the plan's `Interfaces:` line, and the exact-triple duplicate confirmed ORM-unreachable (`IntegrityError` from the DB constraint; no production writer exists to bypass it) while the mirrored half is ORM-reachable and proven end-to-end.

**MEDIUM-1 (real bug, queued as `9-fix`).** `DUPLICATE_RELATION`'s mirrored half is **insertion-order dependent**: the same two facts — one undirected row X–Y plus one directed Y→X of the same type — produce **no issue** when the undirected row was authored ascending, but a `DUPLICATE_RELATION` on the shared key when authored descending. §20.1 says an undirected relation "duplicates its reversed pair", and §5.3:289's *sorted* key exists precisely to make identity order-independent, so an order-dependent verdict defeats the property the sorted key was introduced for. The state is reachable: creating the directed row second passes `full_clean()` (the mirror guard fires only for undirected rows), `create()` never calls `clean()`, and no DB constraint covers the pair. Root cause: `_identities` gives an undirected row `{authored} ∪ {min,max}`, which collapses to one identity when authored ascending. Fix: an undirected row claims **both** orientations (`add((min,t,max)); add((max,t,min))`) and still reports once per shared key; the reviewer verified this changes nothing else (directed pairs stay legal, mirrored pairs still flag once).

**LOW-2/3 (queued with `9-fix`, cheap).** The dangling rule's *target-side* clause is unpinned (a mutation comparing only `source.version_id` survives all 27 tests — the only dangling test uses a foreign *source*), and the keyless detached payload-row branch (`_composed_key → None`, Plan B's bulk-PUT shape) is unpinned (disabling `if key is None:` survives).

**LOW-4/5 (recorded, owned elsewhere).** `ValidationReport` + `blocking_codes()`/`warning_codes()` ship with no test — the aggregator and its tests are Task 12's (`WARNING_CODES == ()` is pinned). The module docstring's "three queries in total, independent of how many relations" is true only for ≥1 relation (a zero-relation version costs 1); the query test pins the ≥1 case.

**INFO/A — cross-product pair semantics (owner note).** `AllowedTypes` is `sources × targets` (spec §5.3's own two-set design), so it cannot express §6.2's `informed-by` list exactly: `project → research-area` is *allowed* though absent from the table, and `uses`/`implements` additionally allow `method → method` / `technology → technology` (blocked only as self-loops). Not a Task 9 defect — the seed/spec vocabulary is the lever.

**INFO/B — visibility scope (accepted, residual risk named).** Judging hidden relations is the literal reading (no §20.1 *relation* row is visibility-qualified, unlike `DANGLING_NODE_HIDDEN_RELATION`), it is recorded in the module docstring and pinned by a test. Residual risk: the gate is stricter than the payload — a hidden invalid row blocks a payload Task 15 would never emit. Recommendation for Plan B's admin copy: say "delete, don't hide" for invalid rows, or qualify the spec rows.

**INFO/C.** Two extra mutations confirmed §6.2's "empty means any" is pinned; one mutation (min/max swap inside the identity tuple) survives as an *equivalent mutant*, which is not a gap. UNVERIFIED: "staged by explicit path" (only the committed path set is observable, and it is exactly the four expected paths).

**`9-cf` CONFIRMED:** `DIRECTION_NOT_OVERRIDABLE` occurs **0 times** in the spec — it is neither in §20.1 nor §20.2. Real spec gap: §20.1 needs a row, because a `directed` contradicting `directed_default` while `overridable_direction` is false violates §5.3:282's field rule. Task 12 must freeze **§20.1 + §20.2 + this code**.

### Task 10 review (frozen `f031b2a`)

Verdict: **CHANGES REQUIRED** — spec compliance **PASS with one named exception**. Independently reproduced: the exact RED string, `64 passed`/`127 passed`, suite `1075 passed, 6 skipped` on both invocations with the parent tree at `1052/6`, ruff/`makemigrations --check`/`check` clean, five LF-only paths +1274/−15 (numstat match), the three-failed/literal-snippet probe, and the evidence's five recorded sha256 prefixes matching the committed files. The reviewer then ran **16 of its own mutations**, all caught, control green, `validation.py` restored after every run.

**Rule-by-rule falsification (all PASS):** parity — blocking not warning, both directions, visible-scoped only, **no cross-locale fallback** (a mutation making "any override cover both locales" is caught *only* by the Phase-0-corrected matrix test, which proves that corrected test is load-bearing rather than decorative), hidden one-sided nodes not gated, whitespace-only overrides not covering, override-on-the-missing-locale clearing while a wrong-locale override does not. Hierarchy — multi-parent legal with `MULTIPLE_PARENTS_ALLOWED` absent from spec *and* repo, only the `hierarchy_role` subset checked, general cycles legal, roots walked in `public_key` order, a single undirected edge walked both ways, three-node undirected loops and self-edges caught, parallel edges not false-positived. Taxonomy — literal per §20.1 (no visibility qualifier) and consistent with Task 9's accepted decision, gate stricter than the payload as already recorded. Canonical refs — a missing key blocks even when both overrides exist, `CANONICAL_SOURCE_UNPUBLISHED` (record-level) and `MISSING_LOCALE_PROJECTION` (locale-level) never alias and co-fire only for different locales, ambiguity surfaces rather than guessing. Grouping — six public validators exist, all eight new codes have live emission sites, no dead-code-only code. Fixtures — additive: `DAG_KEYS` fixed and ascending, `test_validation_relations.py` has **zero changed assert lines**.

**F1 (MEDIUM, queued as `10-fix`) — `HIERARCHY_CYCLE` false negative + row-order dependence.** Reproduced three ways: (a) an undirected `hierarchy_role` A—B plus a directed A→B reports **no** cycle although the module's own documented model says an undirected row contributes both directions; (b) A→C plus undirected C—B—A reports nothing; (c) **the same graph with the two relations inserted in the opposite order flips the verdict** (`[]` vs `[A]`), falsifying the docstring's "never of arrival order or row order". Reachable through an admin-created undirected hierarchy-role type — the fixture itself builds one (`part-of`); the seeded §6.2 vocabulary cannot produce it. Two causes: the arc sort uses *target `public_key` only* (ties fall back to queryset order `(version, sort_order, id)`), and on a GREY hit it skips **every** arc of the entry relation for the whole visit rather than just the immediate walk-back. Fix: discard only the cycle consisting of the single relation walked twice (track the path's relation pks) and add a total tiebreak (relation pk) to the arc sort.

**F3 (LOW, queued with `10-fix`) — one locale's ambiguity masks the other locale's missing projection.** `except AmbiguousCanonicalRef: continue` abandons the whole node: EN ambiguous + FA empty-and-unoverridden yields refs `{AMBIGUOUS}` and parity `[]`. The gate still blocks (AMBIGUOUS is blocking), so this is **report precision** — fix by resolving per missing locale inside the handler.

**F2 (LOW/doc, queued with `10-fix`) — an overstated claim.** The reported node is the one the DFS returns to, which is not always "the first node of that cycle in `public_key` order" (a `…02 ↔ …03` cycle reports `…03`). Deterministic, but the docstring and the test comment overstate it — correct the wording (or the traversal).

**INFO (owner/Task 12):** the taxonomy gate's strictness is an owner decision (qualify §20.1's rows, or bless the strict reading and have Plan B's copy say "delete, don't hide"); and query behaviour is now **measured** — the six validators aggregate to 133 / 333 / 413 queries at 30 / 80 / 100 nodes (≈4.1–4.4 per node, ~93/229/325 ms in-memory) because `resolve_canonical` is called per node per locale; Task 12's aggregator should resolve once per node and share the result, which roughly halves the cost. `validate_hierarchy` (2), `validate_visibility` (2) and `validate_taxonomy` (1) are constant. UNVERIFIED: the implementer's exact mutation script (the reviewer's own 16 cover the same rules).

### R11 (ruling, for the rest of the run): determinism claims must be falsified by permuting row order

Two independent reviews found the **same defect class** in the validation layer: Task 9's `DUPLICATE_RELATION` was insertion-order dependent (MEDIUM-1, fixed in `5edd69a`), and Task 10's `HIERARCHY_CYCLE` is **relation-row-order dependent** with a false negative (F1, queued). Both were invisible to count-based verification and both were found only by constructing the problematic permutation. Ruling: for any validator that sorts or walks relations, a claim of determinism/order-independence must be backed by a test that **permutes the insertion order of the same logical graph and asserts an identical verdict**, not merely by a `public_key`-order assertion on the report. Apply to `10-fix` and to all remaining tasks that touch `validation.py` (12, 15).

### Task 11 review (frozen `11c2579`)

Verdict: **CHANGES REQUIRED** — spec compliance **PASS**, code quality with two MEDIUM findings. Reproduced: the RED (`ModuleNotFoundError`, one collection error), `12 passed` / `144 passed` / `1092 passed, 6 skipped` on both invocations with the parent at `1080/6`, ruff/`migracheck`/`check` clean, exactly 4 paths +1542/−4 (the four deleted lines are docstring-only). **Determinism was attacked and survived**: 7 reproductions of the digest across 2 interpreter processes, 6 `PYTHONHASHSEED` values and a pk-shifted DB; plus reversed arrival orders, six seeded shuffles of node/relation/member lists, groups as shuffled pairs and a reversed dict, relations presented as a `set`, a second version with disjoint pks in the same DB, duplicate rows, a hidden node in the input, and depths under reversal — with **no pk ever appearing in stored keys**. Also verified independently: one single `round(` in the engine; §12.4 centroids from *seed* positions with the ½-mean-spacing displacement cap; `pin_z`-without-x/y raising a real `ValidationError`; **40/40 adversarial pin placements returning byte-verbatim**; step-8 movers excluding pinned nodes; 3 queries + 1 save measured via `CaptureQueriesContext`; a no-op re-apply leaving the stored JSON identical; the 80/150 shape computing in 0.186 s against a 2 s budget; the scale fixture's declared sizes exact, with requests of 80/72/50/30/12/5/1 and a monkeypatched composition all yielding the requested size (so M10's `delta` gap is genuinely fixed), zero canonical rows, and the placeholder/`corrupt_layout` behaviours untouched. The four reinterpretations were judged defensible: §12.2 step 8's rationale and §12.8/§14.3 both require the spiral in **x/y**, making "`y = 0` plane" the erratum; and "one more relaxation pass" reads as the frozen-N routine the same step defines.

**F1 (MEDIUM, queued) — `compute_layout` drains a one-shot `relations` iterator twice.** It is annotated `Iterable[AtlasRelation]` but `_prepare` builds arcs from it and then stores `tuple(relations)`, which is empty for a generator or `filter(...)`; stage 1 then recomputes all depths as 0, the seed stage sorts projects before their parent area, and the public API raises `KeyError: 'research-area-00000001'`. Reproduced with a generator, a `filter(...)`, and both. Fix: materialise `relations = tuple(relations)` once at the top of `_prepare` (which also removes the duplicate arc build).

**F2 (MEDIUM, queued) — the step-8 pin-separation guarantee is falsified by a *legal* pin.** With `research-area-00000002` pinned at a finite in-bounds spot that is ≥ the minimum distance from every other pin, the final layout leaves an unpinned neighbour **5.632 units inside the pin** (and 2.976 inside a second project); the other area at the same spot leaves **9.650**. The fixture's own pins pass and the base layout has zero overlaps, so neither the `_pin_separation` test nor the docstring could see it. **Ruling: fix the code, not the claim (option a)** — §12.2 step 8's *stated* purpose is "so a pin cannot leave an unpinned neighbour overlapping it", so the implementation owes that guarantee: add a deterministic expulsion post-pass that displaces only unpinned movers still inside a pin, exactly to clearance, ties broken by `public_key`, and pin it with the reviewer's counterexample.

**F3 (LOW, queued) — a cyclic input raises a bare `KeyError`** from the seed stage while `hierarchy_depths` returns 0 for the same graph (2-cycle probed). "Validation rejects it first" is true on the service path; either tolerate it deterministically or say so in the docstring.

**F4 (LOW — see the corrected row above).** The determinism probe's *rationale* was wrong: two fresh sqlite test DBs assign the **same** pks (1…72), so "therefore different pks" is false. Conclusion unaffected; the reviewer re-established it with a genuinely shifted pk space.

**F5/F6 (INFO).** The `slow`-marker justification was overstated (an unregistered mark only *warns* under pytest 9.0.3, reproduced) — the `pyproject.toml` registration is still justified hygiene, only the stated reason was wrong; and `apply_layout` joins `source`/`target` that no stage reads while `_Run.nodes` is never read by a stage (cosmetic dead weight). UNVERIFIED: cross-OS/libm trig divergence at the third decimal (not testable on this host) and the implementer's per-item mutation matrix (each rule independently probed anyway).

### Falsification review of the orphaned Task 10 fix (frozen `3b35f16`; scar-hunting, since no implementer probe ever ran)

Verdict: **CHANGES REQUIRED** — but the three closed findings are **genuinely closed**, with far stronger evidence than the implementer could have produced. Method: frozen archive, then an **independent brute-force enumerator** of simple closed walks (500 graphs × 3 relation shuffles → **0 verdict mismatches**, 0 wrongly-named nodes) and a **2 951-graph random sweep** comparing fix vs base (`f031b2a`): `verdict_wrong` **0 vs 74**, `named_not_a_cycle_min` **0 vs 316**, duplicate issues **0 vs 460**. Both directions of the F1 reproduction were rebuilt (including the flip case: `[]` with the undirected row first, `[area-a]` with the directed row first in the base, versus a single stable verdict in the fix), plus a reversed-walk triangle, all permutations × both `sort_order` assignments. F3 closed in both directions with the count verified against the base (one missing locale = 1 issue, both missing = 2 — no off-by-one introduced; the ambiguity is still reported and never swallowed). **No weakened tests**: `git diff f031b2a 3b35f16` shows **0 removed `assert` lines** (11 added) and test functions 22 → 27 with none removed. **Gates** in the frozen tree: `1100 passed, 6 skipped` on both invocations, ruff clean, `No changes detected`, `manage.py check` clean, with the blob hash re-verified after every experiment. **Scar hunt: CLEAN** — no `print`/`pdb`/`breakpoint`/`TODO`/`if False`/`noqa`/bare `assert` in the committed file, no stray probe or `_mutant` artifacts, `_WHITE/_GREY/_BLACK` unreferenced anywhere, exactly three pre-existing functions touched plus the new `_witness_walk`, code sets unchanged, every defined name referenced.

**Mutation table (11 mutants, baseline restored and hash-verified each time):** caught — M1 (witness reusing the excluded relation, 4 tests), M4 (the pre-fix `except …: continue`, 2), M5 (ambiguous locale also reported as missing, 3), M6 (gaps abandoned silently, 2), M7/M8/M11 (reporting origin/`walk[-1]`/goal, 9 each). **Survivors:** **M3 — the self-loop short-circuit is untested by anything in the repo** (removing it changes the verdict on **511/2000 graphs**; `SELF_LOOP_FORBIDDEN` is covered elsewhere but `HIERARCHY_CYCLE`-on-a-self-loop is asserted nowhere); **M9 — the arc sort is genuinely row-order dependent** (a 6-row witness yields 2 verdicts across 31 sampled orders while the committed code is stable over them) yet invisible to the suite, because the R11 permutation test only exercises the two F1 shapes, which are tie-free; **M2** — walking undirected rows one way only is order-independent itself but loses a named cycle on 6/2000 graphs (report completeness). **M10 is an equivalent mutant** (0/2000 differences), confirming the code comment's "no verdict can turn on it".

**New finding of the same class the fix was closing — `validation.py:438`.** The summary line "**`HIERARCHY_CYCLE` for every cycle, at its first node**" is false in general: a minimal 4-node/6-row counterexample (720 orders, one verdict) shows the cycle `{area-a, area-b, area-c}` — whose first node is `…0002` — is **never named**; only `…0001` is. Across the sweep, **13/2951 graphs (≈0.5%) have at least one cycle whose first node goes unnamed** (base 493). The body text and the new F2 test comment *do* match the code (the implementer made the node-identity claim true); only this summary/general-coverage claim is overstated. Same disposition as every other overstated claim in this run: correct the text, and note the deviation from plan Step 3 in the ledger.

**INFO for Task 12.** An `Issue` carries no locale, so a node missing **both** locales emits two indistinguishable `MISSING_LOCALE_PROJECTION` rows (pre-existing, unchanged) — the aggregator should de-duplicate per node+code. And one doc sentence should settle that **two undirected hierarchy rows on one pair *are* reported as a cycle**, since §5.6's "multiple relation types between the same pair" reads as if they were not.

**Process note (the reviewer caught its own mistake):** an early mutation run hit the harness's 180 s foreground timeout **mid-mutation** and left `validation.py` mutated *in its scratch tree*; its driver now restores baseline bytes from the pristine blob and asserts the hash after every step. This is exactly the hazard I checked for in the live tree (which was clean, `21b55367…` on both sides) — and it is why an orphaned commit needs a scar-hunting review rather than a self-report.

### Falsification review of the Task 11 fix (`af3ba7e`) — the expulsion pass FAILS on legal inputs

Verdict: **SPEC COMPLIANCE FAIL, CODE QUALITY FAIL, OVERALL CHANGES REQUIRED.** Everything else in the fix is verified (F1 — including `map`, `set`, `tuple`, `dict.values()`, a queryset, a partially-consumed iterator and a generator of `nodes`, all byte-equal to the list where the base raised `KeyError`; F3 — `hierarchy_depths` and `compute_layout` agree on a 2-cycle and a mixed graph, deterministic and order-independent; F4 — the digest `a31ec1fc…` is identical on `af3ba7e` *and* on `11c2579`, plain and `--shift`, over six hash seeds, with the pk-dependent control differing, so the shift discriminates; F5 — pytest 9.0.3 warns and passes where `--strict-markers` errors; the `_hierarchy_arcs` duplicate build is genuinely gone (2 calls → 1); **0 removed `assert` lines**, +14 added, +3 test functions), and the scar hunt is **CLEAN**. The failure is the new pass itself.

**Counterexamples (exact, reproduced end-to-end through `compute_layout` and independently with duck-typed rows).**
- **C1 — legal two-pin lens:** a 3-node draft with two `project` pins (r = 13.2) at `spot ∓ 20u` (pin-to-pin slack **+13.600**, legal per §12.5) and one unpinned `technology` node (r = 14.4): the final **pin↔unpinned min slack is −15.201**, i.e. the mover is still inside a pin. Arm 25 → −5.201; arm 26.5 → −2.201; with larger radii −22.401 / −12.401 / −9.401. The analytic `2·(wanted − arm)` matches to 1e-3.
- **C2 — 40 000-geometry legal sweep:** **4 778 of 21 945 legal pin sets (21.8%)** end with a mover still inside a pin; **1 788 never converge** (permanent limit cycles, where no round bound helps — one legal case needs 153 rounds, another leaves a residual of 22.796); **13.2%** of all trials are not fixed points.
- **C3 — coincident movers:** twelve movers coincident at a pin centre (r = 13.34) end with min mover↔mover distance **9.245926** (slack −17.434; worst key-index pair's angular gap 0.051086 rad ⇒ slack ≈ −28). The golden-angle ray only makes the placement deterministic — the docstring claims exactly that much, so this is an unresolved **overlap**, not a false claim.
- **C4 — hand-built ping-pong:** for a mover between two pins the shortfall equals the gap exactly (g = 0.16/0.5/2.0/5.0) and **500 rounds still do not converge**, showing the residual scales with the lens rather than being a knife-edge.

**Termination:** the loop always terminates (bounded by `len(pinned)+1`) — so this is not a hang — but it terminates **without being a fixed point** in 13.2% of trials, and the bound is untested by any test (the reviewer's own M6, `range(len(pinned)+1)` → `range(1)`, is caught only because one existing case depends on the extra rounds).

### R12 (ruling, correcting `11-fix-note`): §12.2 step 8's collision pass is GLOBAL, not pin-scoped

Step 5 (`spec:725`) states the operator over "…**each pair** closer than `radius(a) + radius(b) + gap`", and step 8 (`spec:728`) reads "One more relaxation pass **restricted to unpinned nodes**, so a pin cannot leave an unpinned neighbour overlapping it." The only restriction the sentence names is on the **movers**, not on the **pairs**; the purpose clause explains *why* the pass exists (step 7 has just moved the pins), which is why a pin-scoped reading has to import words the spec does not write. Therefore: the pair set is **all pairs**, with the pinned endpoint held still, and two obligations follow — (a) any pair with a pinned endpoint must end at distance ≥ `r_a + r_b` **post-rounding** (the current guard analysis covers rounding correctly); (b) a step whose only job is removing overlap must **not worsen one**, so C4's −2.976 → −9.095 is a regression the fix introduced. My own earlier pin-scoped justification in the `11-fix-note` row was wrong; the implementer's evidence claim that "a relaxation pass after it cannot resolve the residual" is **measurably false**: one extra `_relax → _expel` cycle takes the worst unpinned↔unpinned slack from −9.095 to **+2.9995** and the pin margin from +0.000645 to +2.9855 (seven cycles give exactly **+3.000000000**).

### `11-fix2` (queued behind Task 12 — it owns `layout.py`)

Fix list, in the reviewer's order: (1) `_expel_from_pins` — **delete the "repeat until empty round, ≤ pins+1" loop**; for each unpinned mover inside any pin choose a target that clears **every** pin in one shot, from candidate directions {unit vector away from each violated pin, ordered by `public_key`} ∪ {the golden-angle turn of the mover's index}, taking the first candidate ≥ `r_mover + r_pin + guard` from all pins and otherwise the candidate maximising the minimum clearance — one pass, no oscillation, still a pure function of `public_key`; (2) `_stage_final_collision` — iterate `_relax(unpinned) → _expel_from_pins` until **neither moves** (bounded), so a repair can never leave a pair slacker than it found it (one extra cycle suffices for C4); (3) tests — add C1 (the legal two-pin lens) asserting `min(pin↔unpinned slack) >= 0` **and** that no pair's slack worsens, plus a coincident-mover case; consider a **seeded bounded sweep** (a `slow`-marked test over a few hundred legal geometries, asserting "no mover inside a pin" and "no pair worsens") since the reviewer's 40 000-case sweep is what found the 21.8%, and an untested bound is how this shipped; (4) correct the three false claims — the module docstring's step-8 paragraph (`layout.py:24-26`), the `_expel_from_pins` docstring (`:499-500`, `:507-508`), and the evidence file's "a second relax → expel cycle changes nothing". Also LOW: `_visible_keys` is dead code (pre-existing since `11c2579`).

### Scar-hunting review of the orphaned hierarchy-guard commit (`02d86b1`) — APPROVED

Verdict: **APPROVED.** All four items are closed with tests that are individually load-bearing, the docstring is honest against measured behaviour, **no embedded mutation exists**, and the change is *provably* wording-plus-tests. Reproduced independently: the docstring now reads "per **named** node, at the first node in `public_key` order of the cycle its witness closes" with the "a relation carrying several cycles may name only some" paragraph, and the 4-node/6-row counterexample yields exactly **one** distinct verdict across **all 720 row orders**, naming only `structural-00000001` (an independent oracle finds the cycle `{…0002,…0003,…0004}` unnamed, and the naïve "every cycle" implementation names it); both self-loops (directed and undirected) assert exactly one issue with the right token, and the reviewer verified that *no* prior test asserted `HIERARCHY_CYCLE` for a self-loop anywhere in the repo; the tie-bearing permutation test sees 2 verdicts under a pk-sort mutant and 1 under the committed sort (31 sampled orders); the second-direction shape loses `…0002` under a one-way mutant (its own 3,000-graph generator: 5 cases, 0.17%). Gates re-run rather than read: `1108 passed, 6 skipped` on both invocations, ruff clean, `No changes detected`, `manage.py check` clean; `--collect-only` shows **35** tests in the file (parent 30 → exactly 5 new, suite 1,114) with no shadowed ids; **0 removed `assert` lines**, 27 → 32 test functions; both files and their blobs LF-only (an earlier `grep $'\r'` reading was an MSYS quoting artefact).

**Mutation table (12 mutants, baseline restored and hash-verified):** M1 self-loop short-circuit → exactly the two self-loop tests; M2 one-way undirected walk → exactly its test; M3 arrival-order sort → the tie test + the unnamed-cycle test; M5 reported-node=origin and M6 =goal → 12 tests each; M7 the retired sentence's *behaviour* → the unnamed-cycle and tie tests; M8 dedupe removed → 13 tests; M9 `hierarchy_role` filter dropped → the general-cycle test; M10 direction list inverted → 2; M11 hidden-source exclusion dropped → 2; M12 the walk's own-relation exclusion dropped → 5. **M4 (arc sort drops the `pk` tiebreak) survives the file and the suite but is provably EQUIVALENT, not a gap**: the tiebreak only orders *parallel arcs to the same neighbour* and `_witness_walk` records per-node state, so **0 of 58 240** exhaustive evaluations differ (29 120 four-relation four-node graphs × 2 pk permutations that disagree with arrival order, 17 132 carrying parallel arcs) and 0/2 000 randomized graphs differ — the code comment's "no verdict can turn on it" is literally true.

**Behaviour equivalence PROVEN:** `git diff 3b35f16 02d86b1 -- apps/atlas/validation.py` is **one docstring hunk**; docstring-stripped ASTs are identical (60 285 chars each) and all **26 code objects are byte-identical**; 16 shapes (diamond cycle, both F1 shapes, the tie in both orders, the second-direction shape, F2, undirected pair/path/triangle in both orders, both self-loops, a 2-node cycle, parallel arcs, a cycle+spur) return identical verdicts **and identical named nodes** in parent and frozen trees, with no false positives. The interrupted implementer's probe artefacts are consistent with the commit — its final sha256 for all three watched files equals the committed blobs, so the tree *was* restored before the orchestrator committed.

**Two corrections and one honesty note it filed:** (i) the new docstring cites "ledger row `fix10-b`" for the 13/2951 figure, which actually sits in the fix-10 review block — a one-line repoint, queued as `fix10-c` (it touches `validation.py`, which Task 12 owns right now); (ii) the ledger row's `+9/−3` is corrected to **+8/−1** above; (iii) the **rates are generator-dependent** — the exact `13 of 2 951` and `511/2000` figures come from generators that were not recorded, and this reviewer's own generator produced 3/3 000 (0.10%) and 2 067/3 000 for the same two phenomena. The phenomena are confirmed; the specific percentages should be read as "rare" rather than as fixed constants.

## 7. Cross-repo contract commits

_(Task 19 only — one commit per repository, SHAs recorded here; nothing merged, nothing pushed)_
