# Plan B ledger — `Docs/05-delivery/knowledge-atlas/plans/KNOWLEDGE-ATLAS-V1-PLAN-B-ADMIN-AUTHORING.md`

Plan: **KNOWLEDGE ATLAS V1 — PLAN B (ADMIN AUTHORING)**.
This ledger belongs to Plan B only. Plans A/C/D keep their own.
Execution mode: subagent-driven development (fresh implementer context per task, spec-compliance + code-quality review, fix/re-review loop).

## 1. Baseline

| Fact | Value |
|---|---|
| Backend implementation worktree | `D:/Project/.atlas-worktrees/backend-plan-b` (branch `feat/knowledge-atlas-admin-authoring`, created from `e25933a`) |
| Backend baseline tip / full suite | `e25933a` — 1267 passed / 6 skipped |
| Root (docs) worktree | `D:/Project/.atlas-worktrees/root-plan-b` (branch `docs/knowledge-atlas-plan-b-ledger`) |
| Admin-panel worktree (later tasks) | `D:/Project/.atlas-worktrees/admin-plan-b` (`feat/knowledge-atlas-admin-authoring` at `1c3ef02`) |

## 2. Task log

| Task | Title | Status | Commit(s) | Notes |
|---|---|---|---|---|
| 1 | Admin router skeleton, guards and precondition helper | **COMPLETE** | impl `5f415bc` | Backend worktree clean after commit (0 dirty, exact 3 paths +695: `apps/atlas/api_admin.py` new, `apps/api/admin_api.py` +7 router registration, `tests/test_admin_atlas_api.py` +431 / 22 tests). Focused suite **22 passed** (×10 consecutive runs for stability). Regressions: `test_admin_api_auth.py` + `test_admin_graph_adminside.py` + `test_admin_permission_matrix.py` + `test_admin_openapi.py` + `test_openapi_hash_drift.py` = 85 passed. Full suite **1290 passed / 6 skipped** (baseline 1267; delta includes the 22 Task-1 tests; no pre-existing failure). Gates: `ruff check .` → All checks passed!; `manage.py check` → no issues; `makemigrations --check --dry-run` → No changes detected. |
| 1-r | Independent falsification review of committed tree `5f415bc` | **PASS / APPROVED** | — | Fresh reviewer worked from `git show 5f415bc:<path>` and the plan Task-1 block; re-ran full suite (1290/6, 0 failed). All 15 attack axes held: registration beside `/graph`; auth/OTP/CSRF via shared `admin_common` primitives; `AtlasLayoutIn` intact (`test_invalid_body_still_422` proves invalid body ≠ guard evidence); Atlas wire revision = `version_revision()` output round-tripped through both the helper and the `GET /versions` row; stale/missing/active/draft matrix each breaks exactly one prerequisite; `_atlas_audit` strictly after all guards (failed guard ⇨ no success audit); actions `atlas.layout.replace` / `atlas.node.update`; `admin_common.py` byte-unchanged (no global `_require_if_match` change); no Task-2 scope creep; no Plan-A regression. Two recorded notes (non-blocking): (a) the revision adapter compares the timestamp component EXACTLY instead of the shared ms-rounded primitive — deliberate, ruled: one-millisecond instants must not collide and the wire value is full-microsecond; (b) plan Step 4 asked for `tests/test_admin_permission_matrix.py` Atlas rows, left unchanged — the dedicated file pins the identical guard matrix for Atlas routes (coverage equivalent; cosmetic deviation from the plan's file list). |
| 2 | Version endpoints (create / detail / clone / archive) | **COMPLETE** | impl `3a1ee16` | RED first (8 failed: routes 404) → GREEN **32/32** focused. Regressions: auth/graph/permission-matrix 44 passed. Gates: `ruff check .` All checks passed (after --fix removed 2 unused imports: UNKNOWN_ID + the repeated import block sort); `manage.py check` no issues; `makemigrations --check` No changes detected. Full suite **1300 passed / 6 skipped** (1290 + 10). Commit = exact 2 paths (+215/−2): `api_admin.py` new `AtlasVersionDetailOut` (+ publishedAt), `AtlasVersionCreateIn` (label 1..120), `_version_detail` delegating to the shared `_format_revision`, 4 routes (POST /versions → 201 draft; GET /versions/{id}; POST /clone → services.clone_version; POST /archive: DRAFT → 409 IMMUTABLE_ACTIVE, active → archived 200), audits `atlas.version.<verb>` strictly after guards. Backend worktree clean after commit. |
| 2-r | Independent falsification review of committed tree `3a1ee16` | **PASS / APPROVED** | — | All 7 attack axes held (interface shapes verbatim §200-201, clone preserves pk via services, archive = Plan-A status machine only, counts vs ORM, guard/audit ordering, ratios additive schemas untouched, zero Task-3 creep). Reviewer's own suite run: 1300/6. Two non-blocking notes recorded: (a) `AtlasVersionDetailOut` docstring's "instead of" wording is imprecise (both createdAt and publishedAt are kept — plan-conformant; cosmetic); (b) `test_version_mutations_are_audited`'s archaic ternary reads badly but evaluates correctly (cosmetic, no change required). |
| 3 | Node endpoints with canonical picker | **COMPLETE** | impl `44e1ce1` | RED first (404s on POST/PATCH + 200 on the publicKey attempt) → GREEN **37/37** focused. Gates: `ruff check .` All checks passed; `manage.py check` no issues; `makemigrations --check` No changes detected. Full suite **1305 passed / 6 skipped** (1300 + 5). Commit = exact 2 paths (+590/−13): `api_admin.py` (+469/−12 — AtlasNodePinIn/AtlasNodeOverridesIn/AtlasNodeWriteIn/AtlasNodePatchIn/AtlasNodeRowOut/AtlasCanonicalCandidateOut, `_require_canonical_publishable`, `_as_uuid`, `_validation_from_django` (with model→wire pin remap), `_apply_node_write` (full_clean → save; overrides upsert per locale; group membership replaced transactionally), `_node_row`, 5 node routes + `/canonical-candidates` from `canonical.CANONICAL_SOURCES`), `test_admin_atlas_api.py` (+121: 5 Task-3 tests incl. pin-paired 400 with wire key and the raw-body publicKey rejection). Backend worktree clean after commit. |
| 3-r | Independent falsification review of committed tree `44e1ce1` | **PASS / APPROVED** | — | All 11 attack axes held (routes = plan list; field surface honored incl. transactional membership replace; canonical 3-case gate with the ruled one-locale-allowed reading; publicKey double-locked; pin wire-remap; picker allow-list + public-gated; guard chain order; success-only audits; Task-1/2 schemas byte-identical via `git diff 3a1ee16 44e1ce1`; zero Task-4 creep). Reviewer's own suite run: 1305/6. Non-blocking minors recorded: (a) no dedicated DELETE test (the plan authorizes exactly the shipped list; DELETE shares the same primitives); (b) `_validation_from_django`'s raised line over-indented one space (cosmetic); (c) picker `title__icontains` assumed on all six families — true for the current six (Profile has `title`), belongs in the Task-4 taxonomy pass if the vocabulary grows. |
| 4 | Relation, group and taxonomy endpoints | impl `6d7e661` + fix `7117a28` | **fix APPROVED** | Impl: RED 8 failed (routes 404) → 40/40 focused, suite 1308/6, all gates green. Fix round after **4-r review FAIL/CHANGES REQUIRED**: F1 taxonomy CRUD completed (node-types GET/POST/PATCH; relation-types GET/POST/PATCH/DELETE, all OTP+CSRF, duplicate key 400, in-use retire refused 409); F2 `PATCH /versions/{id}/groups/{key}` (labels map per locale); F3 `TAXONOMY_IN_USE` moved into `admin_common`'s stable wire-code list beside `IMMUTABLE_ACTIVE` (plan-mandated position); F4 both taxonomy deletes wrap `ProtectedError` → 409 `TAXONOMY_IN_USE` (shared `_taxonomy_delete`); F6 membership PUT reorders existing members via per-key `update_or_create` (reverse-order flip pinned); F9 dead helpers deleted, relation list `select_related`. Fix-round gates: focused 47/47, full suite **1315 passed / 6 skipped**, ruff/check/makemigrations green. Commits: 业务 `6d7e661` (+613/−80 incl. `AdminError.issues` additive wire extension + `TAXONOMY_IN_USE`), 修复 `7117a28` (+466/−24). |
| 4-r | Independent falsification review of `6d7e661` (first round) | **FAIL → fixed by `7117a28`** | — | Findings that drove the fix: F1/F2 missing interface routes (taxonomy verbs + group PATCH), F3 wrong code location, F4 unguarded ProtectedError, F6 no membership reorder, F9 dead code + N+1. Verdicts: SPEC COMPLIANCE: FAIL / CODE QUALITY: CHANGES REQUIRED. |
| 4-rr | Scoped re-review of the fix commit `7117a28` | **PASS / APPROVED** | — | All six findings verified CLOSED in the committed tree; zero scope creep (3 paths, all finding-mapped); reviewer's own suite run 1315/6 clean with only the pre-existing ninja deprecation warning. One recorded minor (non-blocking): `list_relations` still fetches source/target nodes per row inside the loop — residual N+1, flagged for the Task-5 touch of this file. |
| 5 | Layout, bulk graph, validation and publish endpoints | impl `b78cc71` + fix `8b2e...`→ actual SHA `8b2f0e3` | **fix APPROVED** | Impl: RED 5 failed (routes 404) → GREEN 52/52, suite 1321/6. Fix round after **5-r review FAIL/CHANGES REQUIRED**: F1 bulk `VALIDATION_BLOCKED` raised INSIDE `transaction.atomic` (reject = rollback, zero writes; `MISSING_LAYOUT` carved out as legitimate post-layout state); F2 activate SUCCESS path pinned end-to-end (bulk clean graph → layout 200 → activate 200 → status active → real PublicationJob row → `atlas.version.activate` audit); F3 dangling bulk relation → 400 `issues[0].code == DANGLING_RELATION_ENDPOINT` (relationKey/nodeKey/messageToken); F4 ruling recorded: bulk = wholesale delete-then-create, `publicKey` identity preserved, DB pks not (docstring honest); F5 status route tested (also asserted the job id equals the activation's own request); F6 `_serviceBlocked` emits the full Plan A issue array via `validation._issue_dict` (dead `_service_error` helper deleted); F8 route-unreferenced `AtlasLayoutIn` schema deleted; F9 untidy test iterations tidied. Bonus: `AtlasBulkNodeIn.overrides` (bulk per-locale overrides, parity with single-node route, needed by the success tests). Fix gates: focused **55/55**, full suite **1323 passed / 6 skipped**, ruff/check/makemigrations green. Commits: `b78cc71` (+486/−23), `8b2f0e3` (+151/−24). |
| 5-r | Independent falsification review of `b78cc71` (first round) | **FAIL → fixed by `8b2f0e3`** | — | F1 bulk reject wrote-before-refusing (data-integrity violation); F2 activate success zero coverage; F3 dangling shape/code divergence; F4 id-preservation pin not implemented as written; F5 status route untested + job lookup not version-scoped; F6 issue stripping; F7 the compliant majority; F8 dead AtlasLayoutIn; F9 test-hygiene remnants. Verdicts: FAIL / CHANGES REQUIRED. |
| 5-rr | Scoped re-review of fix `8b2f0e3` | **PASS / APPROVED** | — | All findings verified CLOSED with the MISSING_LAYOUT carve-out judged justified (layout is recomputed after the graph lands). One addition noted-and-accepted: bulk `overrides` field (parity with single-node route). Status route's `jobId` semantics: reviewer accepted the version-agnostic job lookup shape for this run — recorded for Plan D's interface row (job lookup version-scoping is an owner decision if the wire contract needs it). Reviewer's own suite: 55/55 focused, 1323/6 full. Non-blocking nit recorded: `_serviceBlocked` imports `_issue_dict` locally while the module already carries it as `_atlas_issue_dict` (cosmetic duplication). |
