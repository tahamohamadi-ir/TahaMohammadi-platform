> Unified dispatch dependencies: CA-02. execution-tasks.json is the current authority; legacy dependency prose below is historical.

# CA-03 — Semantic integrated Home hero

Status: **IMPLEMENTED_UNREVIEWED**. Repository owner: **PUBLIC — Front-End/public-site**. Branch suggestion: `cx/concept-v2-ca-03`. Dependencies: CA-02.

## Outcome

Build the new Home hero HTML, move semantic graph inside it and remove the separate Home graph placement in both locales. Lock renderer/controller TypeScript interfaces before graphics workers begin.

## Read only this bounded context

Read workspace AGENTS.md, authority order, manifest, public-site AGENTS.md, ADR-0008, then PACKETS.md common contract and:

DESIGN-SPEC.md §§1–4; src/lib/home-content.ts; src/components/templates/HomeTemplate.astro; src/components/home/HomeHero.astro; src/pages/{fa,en}/index.astro; CA-02 handoff

Paths starting `src/`, `tests/`, `scripts/`, `contracts/`, `package` or `docs/quality/` are relative to **public-site**. `DESIGN-SPEC.md` and V2 files are relative to this central pack. `concepts/` is relative to `Docs/references/frontend-design-authority/`. Inspect actual file existence and Git status before editing. The allowlist includes new proposed files; it is not an assertion that they already exist.

## Exact write allowlist

- `src/components/home/HomeHero.astro`
- `src/components/home/HomeResearchGraph.astro`
- `src/components/hero/HeroGraph.astro`
- `src/components/hero/GraphNodeList.astro`
- `src/components/hero/hero-contract.test.ts`
- `src/lib/visual/scene-contract.ts`
- `src/styles/hero-graph.css`
- `src/pages/fa/index.astro`
- `src/pages/en/index.astro`
- `src/components/home/wp40-home.behavior.test.ts`
- `tests/e2e/ca03-hero-semantic.e2e.ts`
- `src/atlas/sections/FoundationsSection.astro`
- `docs/quality/concept-alignment-v2/CA-03-HANDOFF.md`

Everything else is read-only, including other repositories and central task statuses. Do not edit a shared file from another packet without a coordinator allowlist revision. Do not let a formatter rewrite the entire repository. No secrets, publication, deploy, force push, legacy code copy or invented content. Evidence output images stay under ignored test artifacts; commit only the handoff Markdown and exact allowed source/test files.

## Work sequence

1. Record starting branch/HEAD, dirty status and dependency handoff commits. Work in a clean isolated worktree. Stop if another worker owns an allowlisted file.
2. Fail first on graph outside hero, duplicate graph region, lost research-statement text, missing native node selection and accidental Home portal. Validate no-JS DOM at 390 and 1440 in both locales.
3. Implement the smallest complete outcome. Preserve existing component/route contracts unless this packet and ADR-0008 explicitly change them.
4. Run the focused checks below, inspect rendered output when visual, then run repository-required checks appropriate to the delta. Report unrelated baseline failures separately; do not repair outside scope or weaken assertions.
5. Write `docs/quality/concept-alignment-v2/CA-03-HANDOFF.md` with the handoff fields in PACKETS.md. Commit only the exact repository scope when assigned execution includes commit; never push/merge/deploy yourself.

## Checks

Execute each command separately from public-site (semicolon-separated below is a readable checklist, not a required shell chain):

npm.cmd test -- src/components/hero/hero-contract.test.ts src/components/home/wp40-home.behavior.test.ts; npm.cmd exec playwright test tests/e2e/ca03-hero-semantic.e2e.ts; npm.cmd run build

Use existing preview/backend setup in public-site docs and real fixture routes. No mock personal records in public output. All UI changes include FA/EN and Light/Dark. Standalone packet test files shown above may be created by this packet.

## Done when

One graph under identity-lead, no relationship-graph slot rendered on Home, no portal image on Home, content retained in sensible hierarchy, links/actions usable without JS, scene contracts exported. Old optional template slot can remain for compatibility, unused by Home.

Produce before/after evidence and explicitly label fixture vs published state. A passing automated check never equals owner visual acceptance.

## Stop

Return **`CA-03_HANDOFF_READY`** only with the completed evidence above; otherwise return **`CA-03_BLOCKED`** or **`CA-03_REVISE`** with the concrete reason. Do not begin the next packet or change PUBLIC-190/PUBLIC-350 acceptance.

Coordinator regression scope: `tests/e2e/wp40-home.e2e.ts` (V2 semantic assertions, precedes PU-17-home).
