> Current coordinator revision: [findings and required regressions](../reviews/CATALOG-GRAPH-REVIEW-2026-09-06.md). Existing implementation must be corrected; do not rebuild from scratch.

> Unified dispatch dependencies: CA-01, PU-SYNC-graph. execution-tasks.json is the current authority; legacy dependency prose below is historical.

# CA-02 — Published graph adapter and route resolution

Status: **ACCEPTED_LOCAL**. Repository owner: **PUBLIC — Front-End/public-site**. Branch suggestion: `cx/concept-v2-ca-02`. Dependencies: CA-01.

## Outcome

Adapt the existing public GraphPayloadOut into deterministic, exact-locale semantic data; resolve relatedRecords by eligible record family/ID, never guessed slugs.

## Read only this bounded context

Read workspace AGENTS.md, authority order, manifest, public-site AGENTS.md, ADR-0008, then PACKETS.md common contract and:

DESIGN-SPEC.md §§3,4; src/generated/public-api.ts GraphPayloadOut/GraphNodePublicOut/GraphRelatedRecordOut; src/lib/api/client.ts; src/lib/api/resolve-url.ts; existing family adapters and route helpers (read-only)

Paths starting `src/`, `tests/`, `scripts/`, `contracts/`, `package` or `docs/quality/` are relative to **public-site**. `DESIGN-SPEC.md` and V2 files are relative to this central pack. `concepts/` is relative to `Docs/references/frontend-design-authority/`. Inspect actual file existence and Git status before editing. The allowlist includes new proposed files; it is not an assertion that they already exist.

## Exact write allowlist

- `src/lib/hero-graph-content.ts`
- `src/lib/hero-graph-content.test.ts`
- `tests/fixtures/contracts/hero-graph.json`
- `docs/quality/concept-alignment-v2/CA-02-HANDOFF.md`

Everything else is read-only, including other repositories and central task statuses. Do not edit a shared file from another packet without a coordinator allowlist revision. Do not let a formatter rewrite the entire repository. No secrets, publication, deploy, force push, legacy code copy or invented content. Evidence output images stay under ignored test artifacts; commit only the handoff Markdown and exact allowed source/test files.

## Work sequence

1. Record starting branch/HEAD, dirty status and dependency handoff commits. Work in a clean isolated worktree. Stop if another worker owns an allowlisted file.
2. Fail first on duplicate IDs, dangling edges, non-finite coordinates, absent optional arrays, unknown roles, wrong-locale related records and unresolved IDs. Verify 0/1/3/5/12/50-node fixtures and no fabricated relation/link.
3. Implement the smallest complete outcome. Preserve existing component/route contracts unless this packet and ADR-0008 explicitly change them.
4. Run the focused checks below, inspect rendered output when visual, then run repository-required checks appropriate to the delta. Report unrelated baseline failures separately; do not repair outside scope or weaken assertions.
5. Write `docs/quality/concept-alignment-v2/CA-02-HANDOFF.md` with the handoff fields in PACKETS.md. Commit only the exact repository scope when assigned execution includes commit; never push/merge/deploy yourself.

## Checks

Execute each command separately from public-site (semicolon-separated below is a readable checklist, not a required shell chain):

npm.cmd test -- src/lib/hero-graph-content.test.ts; npm.cmd run lint

Use existing preview/backend setup in public-site docs and real fixture routes. No mock personal records in public output. All UI changes include FA/EN and Light/Dark. Standalone packet test files shown above may be created by this packet.

## Done when

Adapter emits validated nodes and API edges, keeps all eligible nodes, exposes explicit empty/error/unavailable states, and never exposes an unresolved related ID as href. Record whether actual published staging graph was observed; fixture PASS is separate.

Produce before/after evidence and explicitly label fixture vs published state. A passing automated check never equals owner visual acceptance.

## Stop

Return **`CA-02_HANDOFF_READY`** only with the completed evidence above; otherwise return **`CA-02_BLOCKED`** or **`CA-02_REVISE`** with the concrete reason. Do not begin the next packet or change PUBLIC-190/PUBLIC-350 acceptance.
