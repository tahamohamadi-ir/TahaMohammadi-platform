> Unified dispatch dependencies: CA-07, PU-25-public-journey. execution-tasks.json is the current authority; legacy dependency prose below is historical.

# CA-17 — Independent visual and interaction acceptance

Status: **IMPLEMENTED_UNREVIEWED**. Repository owner: **PUBLIC — Front-End/public-site**. Branch suggestion: `cx/concept-v2-ca-17`. Dependencies: CA-07, CA-09, CA-10, CA-11, CA-12, CA-13, CA-14, CA-15, CA-16.

## Outcome

Review the exact integrated public commit against V2 and recorded evidence. Report PASS or REVISE for QA independently from owner acceptance and release readiness.

## Read only this bounded context

Read workspace AGENTS.md, authority order, manifest, public-site AGENTS.md, ADR-0008, then PACKETS.md common contract and:

DESIGN-SPEC.md §9; all CA handoffs; existing performance/no-JS/a11y/visual harness; design-overlay.json

Paths starting `src/`, `tests/`, `scripts/`, `contracts/`, `package` or `docs/quality/` are relative to **public-site**. `DESIGN-SPEC.md` and V2 files are relative to this central pack. `concepts/` is relative to `Docs/references/frontend-design-authority/`. Inspect actual file existence and Git status before editing. The allowlist includes new proposed files; it is not an assertion that they already exist.

## Exact write allowlist

- `tests/e2e/ca17-concept-alignment.visual.e2e.ts`
- `src/test-harness/concept-alignment-captures.ts`
- `docs/quality/concept-alignment-v2/CA-17-HANDOFF.md`

Everything else is read-only, including other repositories and central task statuses. Do not edit a shared file from another packet without a coordinator allowlist revision. Do not let a formatter rewrite the entire repository. No secrets, publication, deploy, force push, legacy code copy or invented content. Evidence output images stay under ignored test artifacts; commit only the handoff Markdown and exact allowed source/test files.

## Work sequence

1. Record starting branch/HEAD, dirty status and dependency handoff commits. Work in a clean isolated worktree. Stop if another worker owns an allowlisted file.
2. Capture the required matrix with same data/viewport/state; check all graph interaction/failure cases, real 200% zoom, keyboard and screen-reader manually. Screenshot assertions must exercise behavior, not merely source-string presence.
3. Implement the smallest complete outcome. Preserve existing component/route contracts unless this packet and ADR-0008 explicitly change them.
4. Run the focused checks below, inspect rendered output when visual, then run repository-required checks appropriate to the delta. Report unrelated baseline failures separately; do not repair outside scope or weaken assertions.
5. Write `docs/quality/concept-alignment-v2/CA-17-HANDOFF.md` with the handoff fields in PACKETS.md. Commit only the exact repository scope when assigned execution includes commit; never push/merge/deploy yourself.

## Checks

Execute each command separately from public-site (semicolon-separated below is a readable checklist, not a required shell chain):

npm.cmd run lint; npm.cmd run format:check; npm.cmd test; npm.cmd run validate:design; npm.cmd run build; npm.cmd run validate:seo; npm.cmd run test:a11y; npm.cmd run test:nojs; npm.cmd run test:performance; npm.cmd exec playwright test tests/e2e/ca17-concept-alignment.visual.e2e.ts

Use existing preview/backend setup in public-site docs and real fixture routes. No mock personal records in public output. All UI changes include FA/EN and Light/Dark. Standalone packet test files shown above may be created by this packet.

## Done when

Paired evidence, hashes, exact commit, accessibility and scene-on/off cost recorded; no acceptance claimed from build alone. If actual published graph or detail data unavailable, state it and do not claim full content-ready acceptance.

Produce before/after evidence and explicitly label fixture vs published state. A passing automated check never equals owner visual acceptance.

## Stop

Return **`CA-17_HANDOFF_READY`** only with the completed evidence above; otherwise return **`CA-17_BLOCKED`** or **`CA-17_REVISE`** with the concrete reason. Do not begin the next packet or change PUBLIC-190/PUBLIC-350 acceptance.
