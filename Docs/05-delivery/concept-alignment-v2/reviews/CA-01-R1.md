# CA-01 — coordinator review R1

Decision: **REVISE**, uncommitted. Do not dispatch dependent packets from HANDOFF_READY alone.

## Verified evidence

The five worker paths match CA-01's allowlist. Existing unrelated documentation changes remain. The focused command was independently rerun: **19 passed, 1 failed**. The failing assertion at `src/foundation.contract.test.ts:142` requires obsolete freeze text. It must not be repaired by reinstating an obsolete policy.

The worker used main rather than the specified isolated worktree and disclosed that deviation. No commit/push occurred. Revision may finish in this same checkout only with exclusive ownership of these five files; subsequent independent work uses isolated checkouts and pinned handoffs.

## Required revision — same five PUBLIC paths

1. Replace the obsolete freeze-text assertion with a check that active PUBLIC dispatch points to EXECUTION/execution-tasks and that retired CA family packets are not separate assignments. Do not change historical milestone text just to satisfy the old test. The test file is already inside the CA-01 allowlist.
2. Remove the permanent `three must not be installed` condition from normal design validation and the permanent absence assertion in foundation tests. CA-01's no-dependency-change rule is a **handoff diff check**, not a lifetime product invariant. Add a negative/positive validator scenario proving that the approved Three.js dependency does not cause validation failure while contradictory portal/hero rules still fail. Use isolated temporary fixtures, not a real package installation in CA-01.
3. Adopt central `design-overlay.json` **2.1.0**. The old central seven-section order was not reconciled with PRODUCT-SPEC F02; this is a coordinator specification correction, not an invented worker change. Exact order: identity-lead-with-graph, audience-paths, selected-work, research-axes, recent-writing, collaboration-cv-contact. Update version/order expectations, consumer hash and template test; preserve pinned historical snapshots.
4. Record no CSS, dependency, route or component changes using actual packet diff evidence. Preserve the original handoff history and append R1 results; use the correct revised version number.

## Acceptance

`npm.cmd run validate:design` and `npm.cmd test -- src/foundation.contract.test.ts src/components/templates/public-160.behavior.test.ts` must both pass. Preserve old snapshot hash checks. Focused lint/format must pass. Include regression evidence for contradictory order/portal and future allowed Three.js. Do not claim visual acceptance.

Return `CA-01_R1_HANDOFF_READY`; central status remains REVISE until coordinator reviews the revision. No next packet, commit, push or deploy is requested here.
