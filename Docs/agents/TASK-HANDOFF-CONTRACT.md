# Task Handoff Contract

A handoff contains:

- task identifier;
- repository and branch;
- exact commit;
- files changed;
- interfaces consumed and produced;
- commands run;
- observed results;
- unresolved risks;
- required next action.

Do not write `done` without an independently checkable result.
