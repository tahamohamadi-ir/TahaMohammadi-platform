# Codex Instructions

Use root and repository `AGENTS.md` files automatically.
Use `rg` for discovery and `apply_patch` for text edits.
Preserve unrelated dirty-worktree changes.
Do not commit or push across repositories in one Git command.
Verify before reporting completion.
