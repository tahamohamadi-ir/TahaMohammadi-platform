# Cursor Instructions

Cursor rules live under each repository `.cursor/rules/`.
The rules are thin adapters to canonical `AGENTS.md` files.
Use repository plans only when they cite an active master task identifier.
Do not accept generated code until repository tests pass.
