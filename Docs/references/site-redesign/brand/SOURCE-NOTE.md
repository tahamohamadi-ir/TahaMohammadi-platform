# Logo source note

- `taha-mark-primary.png` is an exact copy of `apps/web/public/logo.png`.
- `taha-mark-favicon.png` is an exact copy of `apps/web/public/favicon.png`.
- The larger historical/color logo files remain in `Assets/Taha Logo/` and were
  not modified.
- `apps/web/public/favicon.svg` is not a vector version of this mark; it is a
  separate `TM` text graphic and is intentionally excluded.
- No native SVG, AI, EPS, PDF-vector, or editable source matching the approved
  mark was found. Obtain that file from the owner/designer before claiming an
  SVG logo. Do not auto-trace or redraw it without explicit visual approval.
